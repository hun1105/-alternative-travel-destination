# Plan B 코드 사용법 메모

코드가 추가·변경될 때마다 이 문서 끝에 사용법과 검증 결과를 누적한다.

## 2026-07-28 - 관광공사 OpenAPI 클라이언트

### 준비

```powershell
cd outputs\plan_b_api
Copy-Item .env.example .env
```

`.env`에 공공데이터포털 인증키를 입력한다.

```dotenv
KTO_SERVICE_KEY=발급받은_인증키
KTO_MOBILE_APP=PlanB
KTO_MOBILE_OS=ETC
```

### 주요 실행 명령

지역 코드 조회:

```powershell
python -m plan_b_api.cli areas
```

관광지 키워드 검색:

```powershell
python -m plan_b_api.cli keyword "경복궁" --rows 5
```

현재 위치 주변 관광지 검색:

```powershell
python -m plan_b_api.cli nearby --x 126.9770 --y 37.5796 --radius 2000
```

기간 내 축제 검색:

```powershell
python -m plan_b_api.cli festivals --start 20260801 --end 20260831
```

관광지 상세정보 묶음 조회:

```powershell
python -m plan_b_api.cli detail 콘텐츠ID 콘텐츠타입ID
```

한글이 깨지면 다음처럼 실행한다.

```powershell
python -X utf8 -m plan_b_api.cli keyword "경복궁"
```

### 테스트

```powershell
python -m unittest discover -s tests -v
```

현재 테스트 결과: 4개 통과.

### 주의사항

- `.env`는 외부에 공유하거나 Git에 올리지 않는다.
- 상세조회는 후보를 줄인 뒤 호출해 일일 호출량을 절약한다.
- 공사 원본 응답과 가공 데이터는 분리한다.
- 운영 전 공공데이터포털 운영계정을 신청한다.

## 2026-07-28 - 인증키 설정 파일 생성

생성된 파일:

```text
outputs\plan_b_api\.env
```

다음 항목의 등호 뒤에 인증키를 붙여 넣는다.

```dotenv
KTO_SERVICE_KEY=발급받은_인증키
```

저장 후 실제 호출:

```powershell
cd outputs\plan_b_api
python -X utf8 -m plan_b_api.cli keyword "경복궁" --rows 5
```

## 2026-07-28 - HTTP 오류 진단 개선

기존에는 서버가 400·500 오류를 반환하면 상태 코드만 표시했다.
이제 응답 본문의 오류 메시지도 함께 표시한다.

- 인증키는 자동으로 `***` 처리한다.
- XML·HTML 태그는 제거한다.
- 최대 500자까지만 출력한다.

다시 실행:

```powershell
python -X utf8 -m plan_b_api.cli keyword "경복궁" --rows 5
```

대표 오류:

- `SERVICE_KEY_IS_NOT_REGISTERED_ERROR`: 해당 API 활용신청 또는 키 활성화 확인
- `LIMITED_NUMBER_OF_SERVICE_REQUESTS_EXCEEDS_ERROR`: 일일 호출량 초과
- `SERVICE_ACCESS_DENIED_ERROR`: 신청한 API와 호출한 API 불일치 가능

## 2026-07-28 - 관광정보 API 2세대 전환

공공데이터포털의 2025년 5월 변경 공지에 따라 기존 API가 종료됐다.

```text
기존: /B551011/KorService1
현재: /B551011/KorService2
```

오퍼레이션도 `searchKeyword1`에서 `searchKeyword2`처럼 끝 숫자가 변경됐다.
전체 관광정보 호출을 `KorService2`와 `*2` 오퍼레이션으로 수정했다.

환경설정:

```dotenv
KTO_BASE_URL=https://apis.data.go.kr/B551011/KorService2
```

검증 명령:

```powershell
python -X utf8 -m plan_b_api.cli keyword "경복궁" --rows 5
```

## 2026-07-28 - 실제 API 호출 성공

실제 `searchKeyword2` 호출이 성공했다.

```text
resultCode: 0000
resultMsg: OK
검색어: 경복궁
전체 결과: 13건
요청 결과: 5건
```

대표 관광지 결과:

```text
제목: 경복궁
콘텐츠 ID: 126508
콘텐츠 유형 ID: 12
주소: 서울특별시 종로구 사직로 161
경도: 126.97672186606306
위도: 37.576030700049394
```

인증키, `KorService2`, `searchKeyword2`, 한글 응답 처리가 모두 정상이다.

다음 상세정보 검증:

```powershell
python -X utf8 -m plan_b_api.cli detail 126508 12
```

## 2026-07-28 - 상세조회 요청 규격 수정

`detailCommon2`는 구버전의 선택 파라미터를 더 이상 받지 않는다.

삭제한 파라미터:

```text
defaultYN
firstImageYN
areacodeYN
catcodeYN
addrinfoYN
mapinfoYN
overviewYN
```

`detailImage2`에서는 `subImageYN`을 제거했다.

현재 상세 공통정보 요청:

```text
필수 공통값 + contentId
```

API가 오류를 최상위 `resultCode`, `resultMsg`로 반환하는 경우도 처리한다.

재검증:

```powershell
python -X utf8 -m plan_b_api.cli detail 126508 12
```

## 2026-07-28 - 상세정보 묶음 실제 호출 성공

경복궁 콘텐츠로 상세 API 4종 호출이 모두 성공했다.

```text
detailCommon2: 성공
detailIntro2: 성공
detailInfo2: 성공
detailImage2: 성공
```

복구 엔진에서 바로 사용할 수 있는 필드:

| 판단 목적 | 응답 필드 |
|---|---|
| 장소 식별 | `contentid`, `contenttypeid`, `title` |
| 위치·경로 | `addr1`, `mapx`, `mapy` |
| 운영 가능성 | `restdate`, `usetime` |
| 예산 | `infoname=입장료`, `infotext` |
| 주차·편의 | `parking`, `주차요금`, `화장실` |
| 설명 | `overview`, `homepage` |
| 카드 이미지 | `firstimage`, `originimgurl` |
| 저작권 관리 | `cpyrhtDivCd` |

경복궁 확인값:

```text
휴무: 매주 화요일
6~8월 운영: 09:00~18:30, 입장마감 17:30
일반 대인 입장료: 3,000원
화장실: 있음
```

주의:

- 상세 묶음 한 번은 API 호출 4회를 사용한다.
- 목록 후보를 먼저 점수화한 뒤 상위 후보만 상세조회한다.
- 운영시간·휴무·요금 문구는 정규화 파서가 필요하다.
- 이미지 사용 시 `cpyrhtDivCd`를 함께 저장한다.

## 2026-07-29 - 정규화와 사용자별 동적 점수

### 관광지 정규화

실제 상세정보를 복구 엔진용 표준 데이터로 변환한다.

```powershell
python -X utf8 -m plan_b_api.cli normalize 126508 12
```

정규화 결과:

- 운영 월·시간·입장마감
- 정기 휴무 요일
- 일반 성인 입장료와 무료 여부
- 주차·화장실 가능 여부
- 좌표·주소·설명·이미지
- 정규화 신뢰도와 경고

### 사용자 우선순위

각 항목에 1~8순위를 중복 없이 지정한다.
1이 최우선이고 8이 최하위다.

```python
from plan_b_api import UserPriorities

priorities = UserPriorities.from_order(
    "walking_fit",
    "weather_fit",
    "route_time",
    "intent_match",
    "crowd_avoidance",
    "preference_fit",
    "budget_fit",
    "child_fit",
)
```

- 1순위: 30점
- 2순위: 20점
- 3순위: 14점
- 4순위: 10점
- 5순위: 8점
- 6순위: 6점
- 7순위: 4점
- 8순위: 3점
- 데이터 신뢰도: 별도 5점

실행 예제:

```powershell
python -X utf8 -m examples.priority_scoring_example
```

예산을 1순위로 설정하면 저비용 장소가 우선된다.
혼잡 회피를 1순위로 설정하면 한적한 장소가 우선된다.

### 고정 조건

다음 조건은 높은 선호점수로도 무시할 수 없다.

- 위험한 날씨
- 운영하지 않는 장소
- 다음 고정 일정에 도착 불가
- 필수 접근성 미충족
- 사용자가 고정한 예산 초과

### 검증

```powershell
python -m unittest discover -s tests -v
```

현재 전체 테스트: 19개 통과.

## 2026-07-29 - 실제 정규화·동적 점수 검증 성공

경복궁 실제 API 응답을 정규화한 결과:

```text
좌표: 정상
화요일 휴무: 정상 (`closed_weekdays=[1]`)
계절별 운영시간 3개 구간: 정상
입장마감 시각: 정상
일반 성인 입장료 3,000원: 정상
주차·화장실: 정상
저작권 유형 Type1: 정상
정규화 신뢰도: 1.0
경고: 없음
```

사용자 우선순위 변경 검증:

```text
예산 최우선
- 저비용 장소: 84.7
- 한적한 장소: 66.1
- 추천 1위: 저비용 장소

혼잡 회피 최우선
- 저비용 장소: 63.1
- 한적한 장소: 85.0
- 추천 1위: 한적한 장소
```

동일한 후보라도 사용자 우선순위에 따라 최종 추천 순위가 정상적으로
변경된다.

## 2026-07-29 - 점수 입력 자동 생성 모듈

추가 파일:

```text
plan_b_api/signal_builder.py
examples/automatic_signal_example.py
tests/test_signal_builder.py
```

실행:

```powershell
cd "C:\Users\hun\Documents\Codex\2026-07-28\files-mentioned-by-the-user-2026\outputs\plan_b_api"
python -X utf8 -m examples.automatic_signal_example
```

처리 순서:

```text
TourAPI 정규화 장소
+ 도착시각·인원·남은 예산·날씨
+ 이동시간·도보거리·혼잡도·실내비율
→ CandidateSignals 자동 생성
→ 운영·안전·예산·고정일정 필수조건 검사
→ 사용자 우선순위 동적 가중치 적용
→ 최종 점수 또는 제외 이유
```

주요 입력:

- `weather_severity`: 0 맑음, 1 위험 기상
- `indoor_ratio`: 0 완전 야외, 1 완전 실내
- `crowd_level`: 0 한적, 1 매우 혼잡
- `intent_match`, `preference_fit`: 0~1
- `remaining_budget_krw`: 남은 총예산
- `paid_admission_count`: 실제 유료 인원
- `minutes_until_locked_stop`: 다음 고정 일정까지 남은 분
- `budget_is_hard=True`: 예산 초과 후보 즉시 제외

판단 규칙:

- 위험 기상 0.8 이상이고 실내비율 0.2 이하면 제외
- 휴무일·운영시간 밖이면 제외
- 이동+체류+다음 이동+완충시간이 고정 일정까지 남은 시간을 넘으면 제외
- 비용은 입장료×유료 인원+추가 교통비
- 누락값은 중립점수와 `notes`로 표시

전체 테스트:

```powershell
python -m unittest discover -s tests -v
```

검증 결과:

```text
19개 테스트 통과
폭우+야외 후보: 안전 조건으로 제외
폭우+실내 후보: 85.09점, 추천 가능
3인 경복궁형 후보 예상 입장료: 9,000원
```

## 2026-07-29 - 절대 순위 가중치로 변경

기존 1~5 중요도 배율 방식은 제거했다.
이제 사용자가 고른 순위가 배점 순서를 그대로 결정한다.

```python
priorities = UserPriorities.from_order(
    "walking_fit",       # 1순위, 30점
    "weather_fit",       # 2순위, 20점
    "route_time",        # 3순위, 14점
    "intent_match",      # 4순위, 10점
    "crowd_avoidance",   # 5순위, 8점
    "preference_fit",    # 6순위, 6점
    "budget_fit",        # 7순위, 4점
    "child_fit",         # 8순위, 3점
)
```

검증:

```text
도보 1순위: 30점
날씨 2순위: 20점
도보 배점이 날씨보다 높음
중복 순위 입력 시 ValueError
전체 테스트 19개 통과
```

## 2026-07-29 - n개 우선순위 대화형 선택

실행:

```powershell
python -X utf8 -m examples.automatic_signal_example
```

번호를 높은 순서대로 입력한다.

```text
1 날씨, 2 이동시간, 3 원래 경험, 4 혼잡 회피
5 개인 선호, 6 예산, 7 아동, 8 보행 부담
```

사용자 실제 선택 결과:

```text
이동시간: 1순위, 30점
혼잡 회피: 2순위, 20점
개인 선호: 3순위, 14점
미선택 5개 요소: 각각 6.2점
데이터 신뢰도: 5점
합계: 100점
```

실제 후보 결과:

```text
야외 궁궐: 위험 날씨 필수조건으로 제외
실내 박물관: 적격, 78.02점
전체 자동 테스트: 22개 통과
```

## 2026-07-29 - 실제 TourAPI 후보 추천 연결

추가 기능:

```text
현재 좌표 주변 실제 관광지 검색
→ 후보별 상세 API 조회
→ 정규화
→ 사용자 부분 우선순위 적용
→ 필수조건 검사
→ 점수순 추천
```

실행:

```powershell
cd "C:\Users\hun\Documents\Codex\2026-07-28\files-mentioned-by-the-user-2026\outputs\plan_b_api"
python -X utf8 -m plan_b_api.cli recommend-nearby `
  --x 126.9767 --y 37.5760 `
  --radius 1500 --rows 2 `
  --arrival 2026-07-29T14:00 `
  --weather-severity 0.3 `
  --budget 20000 --party-size 2 `
  --max-walking-meters 2000
```

우선순위 입력 예:

```text
2,6,8
```

의미:

```text
1순위 이동시간 30점
2순위 예산 적합 20점
3순위 보행 부담 14점
나머지 선택 항목 동일 배점
```

데이터 구분:

- 실제 TourAPI: 명칭, ID, 타입, 직선거리, 운영시간, 휴무, 입장료
- 추정: 직선거리÷분당 75m 이동시간, 콘텐츠 기반 실내 비율
- 중립값 0.5: 혼잡도, 개인 선호, 원래 경험 유사성
- 미연결: 실제 도보 경로, 실시간 날씨, 실시간 혼잡도

검증:

```text
검색→상세→정규화→점수→정렬 통합 테스트 통과
전체 자동 테스트 24개 통과
Codex 실행 환경의 외부 소켓 제한으로 실 API 최종 호출은 사용자 터미널에서 실행 필요
```

## 2026-07-29 - 축제·행사 정규화 확장

새 정규화 필드:

```text
event_start_date: 행사 시작일
event_end_date: 행사 종료일
session_times: 회차별 시작시각
reservation_required: 사전예약 필요 여부
raw_booking: 예약 관련 API 원문
```

추가 인식:

- `eventstartdate`, `eventenddate`
- `playtime`의 `1회 18:40 / 2회 19:40`
- `usetimefestival`, `이용요금`, `이용료`, `행사요금`
- `성인 5천원`, `1인 60,000원`
- `사전예약`, `예약 필수`, `예매`, `예약제`

판정 변화:

- 행사 기간 전후 후보는 운영하지 않는 장소로 제외
- 행사 기간 안이지만 종료시각이 없으면 운영 여부 보류
- 인식된 행사요금은 인원수와 남은 예산 점수에 반영
- 행사 기간·회차·예약 여부를 추천 결과에 출력

검증:

```text
경복궁 별빛야행형 테스트 데이터
기간: 2026-04-02~2026-05-17
회차: 18:40, 19:40
요금: 60,000원
사전예약: 필요
전체 자동 테스트: 26개 통과
```

## 2026-07-29 - 회차 시작·종료시각 구분 수정

실제 별빛야행 응답:

```text
1회 18:30~20:20
2회 19:40~21:30
```

수정 전에는 네 시각을 모두 회차로 표시했다.
수정 후에는 시작시각만 회차로 표시한다.

```text
운영 회차: 18:30, 19:40
운영 구간: 18:30~20:20, 19:40~21:30
```

추가 변화:

- 복수 회차 운영 구간 모두 운영 여부 판정
- 혼잡도 중립값 안내 중복 제거
- 전체 자동 테스트 27개 통과

## 2026-07-29 - 기상청 단기예보 자동 연결

사용 API:

```text
기상청_단기예보 조회서비스
VilageFcstInfoService_2.0/getVilageFcst
```

환경변수:

```text
KMA_SERVICE_KEY=공공데이터포털_일반_인증키
```

날씨 단독 확인:

```powershell
python -X utf8 -m plan_b_api.cli weather --x 126.9767 --y 37.5760 --at 2026-07-29T15:00
```

자동 추천:

```powershell
python -X utf8 -m plan_b_api.cli recommend-nearby `
  --x 126.9767 --y 37.5760 `
  --radius 1500 --rows 3 `
  --arrival 2026-07-29T15:00 `
  --budget 20000 --party-size 2
```

`--weather-severity`를 생략하면 자동 조회한다.

- 기온
- 강수확률·강수형태·강수량
- 하늘상태
- 습도
- 풍속

자동 처리:

```text
위경도 → 기상청 5km 격자
→ 최신 발표 단기예보
→ 도착시각 예보 선택
→ 강수·강풍·폭염·한파 위험도 0~1
→ 추천 날씨 점수·안전조건 반영
```

수동값은 계속 사용할 수 있다.

```powershell
--weather-severity 0.8
```

검증:

```text
경복궁 좌표 → 기상청 격자 60,127
폭우 예보 → 위험도 0.8 이상
전체 자동 테스트 31개 통과
```

## 2026-07-29 - 건청궁 실내 오분류 수정

문제:

```text
건청궁 소개문에 국립고궁박물관이 포함됨
→ 소개문의 '박물관'을 감지
→ 건청궁을 실내비율 0.95로 오분류
→ 폭염인데 날씨 적합도 97% 출력
```

수정:

```text
장소명 분류를 소개문보다 우선
건청궁·공원·산·해변 등 → 야외 우선
박물관·미술관·전시장 등 → 실내 우선
장소명이 불명확할 때만 콘텐츠 타입·소개문 사용
```

예상 변화:

```text
건청궁 실내비율: 0.15
날씨 위험도 0.6일 때 날씨 적합도: 0.49
```

전체 자동 테스트: 32개 통과
## 2026-07-29 - 별도 API 최적화 추천 명령 추가

기존 `recommend-nearby`는 유지한다.

새 명령:

```powershell
cd "C:\Users\hun\Documents\Codex\2026-07-28\files-mentioned-by-the-user-2026\outputs\plan_b_api"

python -X utf8 -m plan_b_api.cli recommend-nearby-optimized `
  --x 126.9767 --y 37.5760 `
  --radius 3000 --search-rows 20 `
  --eligible-count 3 --max-detail-calls 12 `
  --arrival 2026-07-29T15:00 `
  --budget 20000 --party-size 2
```

우선순위 입력 예:

```text
2,4,6
```

현재 6개 항목:

```text
1 날씨 적합
2 이동시간
3 혼잡 회피
4 예산 적합
5 아동 적합
6 보행 부담
```

개인 선호와 원래 여행 경험 유사성은 점수에서 제거했다.
선택하지 않은 항목은 같은 가중치를 받는다.

최적화 동작:

```text
주변 목록은 1회 조회
중복 콘텐츠 ID 제거
가까운 후보부터 상세 조회
적격 후보 3개 확보 시 중단
상세정보 6시간 캐시
날씨 20분 캐시
API 오류 시 만료 캐시 임시 사용
호출·캐시·중복·실패 통계 출력
```

기본 캐시 파일:

```text
.cache\plan_b_api.sqlite3
```

검증:

```powershell
python -m unittest discover -s tests -v
```

결과: 자동 테스트 34개 통과.

## 2026-07-29 - 추천 설명과 판단 신뢰도 강화

실행 명령은 기존 최적화 명령과 같다.

```powershell
python -X utf8 -m plan_b_api.cli recommend-nearby-optimized `
  --x 126.9767 --y 37.5760 `
  --radius 3000 --search-rows 20 `
  --eligible-count 3 --max-detail-calls 12 `
  --arrival 2026-07-29T15:00 `
  --budget 20000 --party-size 2
```

후보별 추가 출력:

```text
판단 신뢰도: 0~100%와 높음·보통·낮음
운영 확인: 도착 시 운영 확인·미운영 확인·운영 여부 미확인
상세정보 출처: 실시간 API·유효 캐시·만료 캐시
실제 데이터: API 직접 확인값
추정 데이터: 거리·유형 기반 계산값
중립 처리: 누락으로 기본값을 적용한 항목
정규화 경고: 운영시간·휴무·요금·좌표 누락
```

판단 신뢰도 산식:

```text
정규화 완성도 × 60%
+ 핵심 판단정보 확보율 × 40%
```

신뢰도 등급:

```text
80~100%: 높음
60~79%: 보통
0~59%: 낮음
```

API 통계도 상세·날씨 캐시로 분리한다.

```text
상세 캐시 적중
날씨 캐시 적중
상세 만료 캐시
날씨 만료 캐시
```

검증 결과: 자동 테스트 34개 통과.

## 2026-07-29 - TMAP 실제 보행 경로 연결

`.env`에 SK open API 앱 키를 추가한다.

```text
TMAP_APP_KEY=발급받은_TMAP_앱키
```

키가 있으면 기존 최적화 명령에서 자동 사용한다.

```powershell
python -X utf8 -m plan_b_api.cli recommend-nearby-optimized `
  --x 126.9767 --y 37.5760 `
  --radius 3000 --search-rows 20 `
  --eligible-count 3 --max-detail-calls 12 `
  --arrival 2026-07-29T15:00 `
  --budget 20000 --party-size 2 `
  --route-mode auto
```

경로 모드:

```text
auto: 키가 있으면 TMAP, 없으면 직선거리 추정
tmap: TMAP 키가 없으면 오류
estimated: 항상 직선거리 추정
```

경로 API 단독 확인:

```powershell
python -X utf8 -m plan_b_api.cli walking-route `
  --start-x 126.9767 --start-y 37.5760 `
  --end-x 126.9770 --end-y 37.5738 `
  --end-name "대한민국역사박물관"
```

추천 출력 추가:

```text
실제 도보 경로: 거리와 시간
경로 출처: 실시간 TMAP·유효 캐시·만료 캐시·직선거리 추정
보행 경로 API 호출 수
보행 경로 캐시 적중 수
보행 경로 대체 처리 수
```

경로 캐시 유효기간은 24시간이다.
필수조건에서 이미 제외된 후보는 경로 API를 호출하지 않는다.
TMAP 장애 시 만료 캐시를 사용하고, 캐시도 없으면 추정값으로 계속한다.

검증 결과: 자동 테스트 37개 통과.

## 2026-08-03 - TMAP 실제 연동 완료

단독 보행 경로 호출 결과:

```text
출발: 126.9767, 37.5760
도착: 126.9770, 37.5738
실제 보행 거리: 250m
예상 소요시간: 215초, 약 3.6분
```

전체 추천 최초 실행 결과:

```text
목록 API: 1회
상세 API: 8회
날씨 API: 1회
보행 경로 API: 2회
실패: 0회
```

동일 명령 재실행 결과:

```text
실제 API 호출: 1회
목록 API: 1회
상세 API: 0회
날씨 API: 0회
보행 경로 API: 0회
상세 캐시 적중: 8회
날씨 캐시 적중: 1회
보행 경로 캐시 적중: 3회
실패: 0회
```

TMAP 실제 경로가 이동시간·보행 부담 점수에 반영되는 것을 확인했다.
동일 좌표 후보는 같은 보행 경로 캐시를 공유한다.

## 2026-08-03 - 백엔드 JSON API 서버 추가

서버 실행:

```powershell
cd "C:\Users\hun\Documents\Codex\2026-07-28\files-mentioned-by-the-user-2026\outputs\plan_b_api"
.\서버_실행.ps1
```

실행 정책 오류가 나면 직접 실행한다.

```powershell
python -X utf8 -m plan_b_api.server --host 127.0.0.1 --port 8000
```

다른 PowerShell 창에서 전체 확인:

```powershell
cd "C:\Users\hun\Documents\Codex\2026-07-28\files-mentioned-by-the-user-2026\outputs\plan_b_api"
.\API_확인.ps1
```

제공 API:

```text
GET  /health                 인증키 연결 상태
GET  /priorities             우선순위 6종
GET  /weather                기상청 예보
GET  /walking-route          TMAP 실제 보행 경로
GET  /places/{content_id}    관광지 정규화 상세
POST /recommendations        실제 추천 3개
```

추천 요청 예시:

```json
{
  "longitude": 126.9767,
  "latitude": 37.5760,
  "priorities": [2, 4, 6],
  "radius": 3000,
  "search_rows": 20,
  "eligible_count": 3,
  "max_detail_calls": 12,
  "budget": 20000,
  "party_size": 2,
  "route_mode": "tmap"
}
```

실제 검증 결과:

```text
HTTP 상태: 200
관광공사 연결: 정상
기상청 연결: 정상
TMAP 연결: 정상
추천 결과: 3개
추천 API 실패: 0개
캐시 재실행 실제 API 호출: 1회
```

자동 테스트 40개 통과.

## 2026-08-03 - MVP 시나리오 테스트 추가

실행:

```powershell
cd "C:\Users\hun\Documents\Codex\2026-07-28\files-mentioned-by-the-user-2026\outputs\plan_b_api"
.\시나리오_테스트.ps1
```

직접 실행:

```powershell
python -X utf8 -m plan_b_api.scenario_runner
```

검증 시나리오:

```text
1. 폭염 시 야외 후보 제외
2. 비 오는 날 실내 우선
3. 야간 운영 종료 후보 제외
4. 정기 휴무일 후보 제외
5. 종료된 행사 제외
6. 필수 예산 초과 후보 제외
7. 아동 동반 편의시설 반영
8. 도보 제한 점수 반영
9. API 장애 시 만료 캐시 복구
10. 반복 호출 캐시 사용
```

현재 결과: 10/10개 통과.

## 2026-08-03 - 추천 품질 확정 규칙 3종 반영

아동 적합:

```text
우선순위에서 아동 적합을 선택함 → 선택 순위 배점 적용
아동 적합을 선택하지 않음       → 가중치 0점
남은 점수는 다른 미선택 항목에 균등 배분
```

추천 업종 필터:

```text
허용: 관광지(12), 문화시설(14), 축제(15), 여행코스(25), 레포츠(28)
제외: 숙박(32), 쇼핑(38), 음식점(39), 기타 미지원 유형
```

마지막 입장시간:

```text
도착시각 < 입장마감 → 운영 중
도착시각 ≥ 입장마감 → 도착 시 운영하지 않음으로 제외
입장마감이 없으면 폐장시간 사용
```

운영시간 미확인 후보는 기존처럼 적격 상태를 유지하고 별도 경고한다.

검증:

```text
자동 테스트 43개 통과
MVP 시나리오 10/10개 통과
```

실제 API 검증:

```text
두가헌 레스토랑(39) 추천 목록 제외 확인
대체 3위로 세종대왕 동상(12) 추천 확인
아동 적합 미선택 시 출력에 '점수 제외' 표시
```

## 2026-08-03 - 식사 일정 공백 시 음식점 동적 허용

> 2026-08-04 정책 변경으로 폐기되었다. 현재는 음식점과 쇼핑을 항상 포함한다.

기본 추천에서는 음식점 유형 `39`를 제외한다.
여행 계획에서 식사 항목이 사라진 경우에만 음식점을 허용한다.

주변 관광 후보와 음식점을 함께 평가:

```powershell
python -X utf8 -m plan_b_api.cli recommend-nearby-optimized `
  --x 126.9767 --y 37.5760 `
  --include-restaurants
```

식사 항목만 복구:

```powershell
python -X utf8 -m plan_b_api.cli recommend-nearby-optimized `
  --x 126.9767 --y 37.5760 `
  --content-type-id 39 `
  --include-restaurants
```

JSON 요청:

```json
{
  "longitude": 126.9767,
  "latitude": 37.5760,
  "include_restaurants": true,
  "content_type_id": "39",
  "priorities": [2, 4, 6]
}
```

음식점 전용 필드도 정규화한다.

```text
opentimefood   → 운영시간
restdatefood   → 휴무일
parkingfood    → 주차 여부
reservationfood → 예약 필요 여부
```

검증 결과:

```text
기본 추천 음식점 제외: 통과
식사 복구 음식점 포함: 통과
자동 테스트 45개 통과
MVP 시나리오 10/10개 통과
```

## 2026-08-03 - SK 실시간 장소 혼잡도 연결

> 2026-08-04부터 추천 연결은 폐기했으며 단독 개발 도구만 유지한다.

공식 API:

```text
지원 장소: GET https://apis.openapi.sk.com/puzzle/place/meta/pois
실시간 혼잡도: GET https://apis.openapi.sk.com/puzzle/place/congestion/rltm/pois/{poiId}
헤더: appKey
```

혼잡도 단계와 점수 변환:

```text
1단계 여유       → 혼잡도 0.000 → 혼잡 회피 100%
2단계 보통       → 혼잡도 0.333 → 혼잡 회피 67%
3단계 혼잡       → 혼잡도 0.667 → 혼잡 회피 33%
4단계 매우 혼잡 → 혼잡도 1.000 → 혼잡 회피 0%
```

앱키 설정:

```text
SK_CROWD_APP_KEY=발급받은_앱키
```

`SK_CROWD_APP_KEY`가 없으면 `TMAP_APP_KEY`를 사용한다.
단, 같은 SK 앱에 장소 혼잡도 상품 권한이 있어야 한다.

단독 확인:

```powershell
cd "C:\Users\hun\Documents\Codex\2026-07-28\files-mentioned-by-the-user-2026\outputs\plan_b_api"
python -X utf8 -m plan_b_api.cli crowd --name "경복궁"
```

추천 확인:

```powershell
python -X utf8 -m plan_b_api.cli recommend-nearby-optimized `
  --x 126.9767 --y 37.5760 `
  --arrival 2026-08-03T15:00 `
  --crowd-mode auto --max-crowd-calls 3
```

JSON API:

```text
GET /crowd?name=경복궁
GET /crowd?poi_id=1172091
POST /recommendations 본문: "crowd_mode": "auto", "max_crowd_calls": 3
```

호출량 보호:

```text
지원 장소 목록 24시간 캐시
실시간 혼잡도 10분 캐시
추천 1회 실시간 호출 최대 3건
이름 정확 일치 후보만 호출
미지원·실패 시 혼잡 회피 50% 중립값
```

현재 실제 호출 결과:

```text
403 INVALID_API_KEY
원인: 기존 TMAP 앱키에 장소 혼잡도 상품 권한 없음
조치: SK open API 앱에 장소 혼잡도 상품 추가 후 앱키 저장
```

검증:

```text
자동 테스트 48개 통과
MVP 시나리오 10/10개 통과
```

## 2026-08-04 서울시 실시간 혼잡도 연결

서울시 공식 121개 장소 영역 폴리곤으로 후보 좌표를 영역에 매칭한다.
서울시 혼잡도를 우선 사용하고, 영역 밖에서는 SK 혼잡도를 대체 사용한다.

인증키:

```text
SEOUL_OPEN_API_KEY=발급받은_인증키
```

단독 확인:

```powershell
cd "C:\Users\hun\Documents\Codex\2026-07-28\files-mentioned-by-the-user-2026\outputs\plan_b_api"
python -X utf8 -m plan_b_api.cli seoul-crowd --x 126.9767 --y 37.5760
```

간편 실행:

```powershell
.\혼잡도_확인.ps1
```

추천 확인:

```powershell
python -X utf8 -m plan_b_api.cli recommend-nearby-optimized `
  --x 126.9767 --y 37.5760 `
  --arrival 2026-08-04T15:00 `
  --crowd-mode auto --max-seoul-crowd-calls 10
```

혼잡도 모드:

```text
auto  서울시 키가 있으면 영역 혼잡도 사용, 없으면 중립 처리
seoul 서울시 영역 혼잡도 필수 사용
off   혼잡도 API 미사용
```

실제 호출 확인:

```text
영역: 경복궁(POI008)
상태: 약간 붐빔
예상 인구: 1,500~2,000명
정규화 혼잡도: 0.667
API 호출: 1회
```

검증:

```text
자동 테스트 52개 통과
MVP 시나리오 10/10개 통과
```

## 2026-08-04 영역 혼잡도 전용 정책

추천 엔진에서 SK 장소 혼잡도 자동 대체를 제거했다.
서울시 121개 영역 혼잡도만 사용하고 영역 밖·호출 실패는 중립값으로 처리한다.

점수 반영:

```text
점수용 혼잡도 = 서울시 영역 혼잡도 60% + 중립값 40%
여유(0.0) → 점수용 혼잡도 0.2
중간(0.5) → 점수용 혼잡도 0.5
붐빔(1.0) → 점수용 혼잡도 0.8
```

항상 표시되는 주의문:

```text
서울시 혼잡도는 영역별 데이터이므로 개별 장소·건물의 실제 혼잡도와 다를 수 있습니다.
```

추천 실행:

```powershell
python -X utf8 -m plan_b_api.cli recommend-nearby-optimized `
  --x 126.9767 --y 37.5760 `
  --arrival 2026-08-04T15:00 `
  --crowd-mode auto --max-seoul-crowd-calls 10
```

혼잡도 모드:

```text
auto  서울시 키가 있으면 영역 혼잡도 사용, 없으면 중립 처리
seoul 서울시 영역 혼잡도 필수 사용
off   혼잡도 API 미사용
```

검증:

```text
자동 테스트 53개 통과
MVP 시나리오 10/10개 통과
```

## 2026-08-04 쇼핑·음식점 기본 포함

추천 후보에 관광공사 쇼핑 유형 `38`과 음식점 유형 `39`를 항상 포함한다.
숙박 유형 `32`는 계속 제외한다.

기본 추천:

```powershell
python -X utf8 -m plan_b_api.cli recommend-nearby-optimized `
  --x 126.9767 --y 37.5760 `
  --arrival 2026-08-04T15:00
```

음식점만 검색:

```powershell
python -X utf8 -m plan_b_api.cli recommend-nearby-optimized `
  --x 126.9767 --y 37.5760 --content-type-id 39
```

쇼핑만 검색:

```powershell
python -X utf8 -m plan_b_api.cli recommend-nearby-optimized `
  --x 126.9767 --y 37.5760 --content-type-id 38
```

기존 `--include-restaurants` 옵션은 호환 목적으로만 남아 있으며 입력하지 않아도 된다.

검증:

```text
자동 테스트 54개 통과
MVP 시나리오 10/10개 통과
```

## 2026-08-04 판단 신뢰도·필수 제한·추천 근거 개선

낮은 신뢰도 후보가 높은 기본 점수만으로 상위에 오르는 문제를 수정했다.

신뢰도 산식:

```text
판단 신뢰도 = 정규화 완성도 60% + 핵심 판단정보 확보율 40%
신뢰도 감점 = (1 - 판단 신뢰도) × 15점
최종 점수 = 기본 점수 - 신뢰도 감점
```

예시:

```text
신뢰도 100% → 0점 감점
신뢰도 80%  → 3점 감점
신뢰도 60%  → 6점 감점
신뢰도 40%  → 9점 감점
```

도보 필수 제한:

```text
실제 또는 추정 도보시간 15분 이내: 통과
15분 초과: 강제 제외
제외 이유: 도보 이동시간이 최대 허용시간을 초과함
```

대중교통·자차 제한:

```text
정책값: 30분
현재 상태: 이동수단별 경로 API가 없어 저장만 하고 강제 제외하지 않음
적용 시점: 대중교통·자동차 경로 API 연결 후
```

예산 정책:

```text
우선순위·점수·필수 제한에서 임시 제외
입장료 원본 정보는 계속 보관
```

마지막 입장시간이 필요한 이유:

```text
장소 폐장시간보다 매표·입장 종료가 빠를 수 있다.
폐장 전 도착해도 입장마감 후라면 실제 방문이 불가능하다.
따라서 운영 필터는 폐장시간보다 마지막 입장시간을 우선한다.
```

추천 결과 출력:

```text
기본 점수
판단 신뢰도와 등급
신뢰도 감점
최종 점수
기본 점수 항목별 기여
실제·추정·중립·점수 제외 데이터
필수조건 제외 이유
```

사용자 선택형 필수 제한은 아직 추가하지 않았다.
이후 각 조건에 `필수` 토글을 두고, 선택 시 강제 제외하며 미선택 시 점수 감점만 적용한다.

우선순위는 예산을 제외한 5개다.

```text
1 날씨 적합
2 이동시간
3 혼잡 회피
4 아동 적합
5 보행 부담
```

실행:

```powershell
python -X utf8 -m plan_b_api.cli recommend-nearby-optimized `
  --x 126.9767 --y 37.5760 `
  --arrival 2026-08-04T15:00 `
  --max-walking-minutes 15 `
  --max-transport-minutes 30
```

검증:

```text
자동 테스트 57개 통과
MVP 시나리오 10/10개 통과
```

## 2026-08-04 서울시 대중교통 환승경로 연결

TMAP은 보행 경로에만 유지한다.
대중교통은 서울특별시 공식 환승경로 조회 서비스를 사용한다.

처리 순서:

```text
도보 15분 이내 → 도보 후보로 평가
도보 15분 초과 → 서울시 버스·지하철 환승경로 조회
대중교통 30분 이내 → 후보 복구
대중교통 30분 초과 → 강제 제외
환승경로의 보행시간 15분 초과 → 강제 제외
```

인증키:

```text
공공데이터포털에서 "서울특별시_대중교통환승경로 조회 서비스" 활용신청
.env에 SEOUL_TRANSIT_SERVICE_KEY 저장
같은 포털 일반 인증키면 기존 KTO_SERVICE_KEY 자동 재사용 가능
```

단독 확인:

```powershell
cd "C:\Users\hun\Documents\Codex\2026-07-28\files-mentioned-by-the-user-2026\outputs\plan_b_api"
.\대중교통_경로_확인.ps1
```

직접 실행:

```powershell
python -X utf8 -m plan_b_api.cli seoul-transit-route `
  --start-x 126.9767 --start-y 37.5760 `
  --end-x 127.0095 --end-y 37.5665
```

추천 실행:

```powershell
python -X utf8 -m plan_b_api.cli recommend-nearby-optimized `
  --x 126.9767 --y 37.5760 `
  --arrival 2026-08-04T15:00 `
  --max-walking-minutes 15 `
  --max-transport-minutes 30 `
  --transit-mode auto `
  --max-seoul-transit-calls 3
```

호출 최적화:

```text
도보 15분 초과 후보에만 호출
추천 1회 기본 최대 3회
경로 결과 30분 캐시
통계에 실제 호출·캐시 적중·실패 표시
```

판단 신뢰도 최대 감점은 15점으로 유지한다.

실제 호출 검증 시 현재 실행 환경의 외부 DNS 차단으로 네트워크 호출은
완료되지 않았다. 파서·캐시·제한 정책은 모의 API 응답으로 검증한다.

최종 검증:

```text
자동 테스트 61개 통과
MVP 시나리오 10/10개 통과
CLI 도움말 정상
판단 신뢰도 최대 감점 15점 유지
```

## 2026-08-04 후보 방문 후 다음 일정 도착 가능성

여행 계획 전체 모델을 만들기 전에 다음 일정 제약을 추천 엔진에 먼저
연결했다.

판단식:

```text
필요시간 = 현재→후보 이동시간
         + 후보 예상 체류시간
         + 후보→다음 일정 이동시간
         + 안전 여유시간

필요시간이 다음 일정까지 남은 시간 이내면 통과한다.
초과하면 "다음 고정 일정에 제시간 도착 불가"로 제외한다.
```

다음 구간 경로 정책:

```text
TMAP 실제 도보 경로 우선
도보 15분 초과 시 서울시 버스·지하철 환승경로 확인
대중교통 30분 초과 시 연결 불가
API가 없으면 직선거리 기반 도보시간 추정
후보 좌표가 없으면 연결 불가
```

실행:

```powershell
cd "C:\Users\hun\Documents\Codex\2026-07-28\files-mentioned-by-the-user-2026\outputs\plan_b_api"
.\다음_일정_도착_테스트.ps1
```

핵심 입력값:

```text
--arrival: 문제 발생 후 추천 시작시각
--next-x, --next-y: 다음 일정 좌표
--next-arrival: 다음 일정 도착 마감시각
--next-title: 다음 일정 이름
--visit-minutes: 후보 예상 체류시간, 기본 60분
--schedule-buffer-minutes: 안전 여유시간, 기본 10분
```

추천 결과에 다음 항목을 추가했다.

```text
다음 일정 이름과 마감시각
후보→다음 일정 이동수단·소요시간·출처
예상 다음 일정 도착시각
남는 여유시간
도착 가능 여부와 제외 이유
```

JSON API `POST /recommendations` 입력명:

```json
{
  "next_longitude": 127.0095,
  "next_latitude": 37.5665,
  "next_arrival_time": "2026-08-04T17:00:00",
  "next_title": "동대문디자인플라자 예약",
  "visit_minutes": 60,
  "schedule_buffer_minutes": 10
}
```

검증:

```text
자동 테스트 64개 통과
MVP 시나리오 10/10개 통과
늦는 후보 강제 제외 통합 테스트 통과
판단 신뢰도 최대 감점 15점 유지
```

## 2026-08-04 TMAP 자동차·도보 경로 통합

기존 `TMAP_APP_KEY`로 도보와 자동차 경로를 모두 연결했다.

추천 경로 선택 순서:

```text
도보 15분 이내 → 도보 사용
도보 15분 초과 → TMAP 자동차 경로 확인
자동차 30분 이내 → 자동차 사용
자동차 30분 초과·실패 → 서울시 대중교통 확인
모두 불가능 → 후보 제외
```

자동차 경로는 교통상황 변화가 있으므로 5분 캐시한다.
도보 경로는 24시간 캐시한다.

실행:

```powershell
cd "C:\Users\hun\Documents\Codex\2026-07-28\files-mentioned-by-the-user-2026\outputs\plan_b_api"
.\도보_자동차_경로_확인.ps1
```

자동차만 확인:

```powershell
python -X utf8 -m plan_b_api.cli car-route `
  --start-x 126.9767 --start-y 37.5760 `
  --end-x 127.0095 --end-y 37.5665 --end-name "DDP"
```

실제 호출 결과:

```text
구간: 경복궁 인근 → DDP
도보: 3,873m / 54.1분
자동차: 4,450m / 25.63분
예상 택시비: 10,500원
자동차 30분 제한: 통과
```

JSON API:

```text
GET /walking-route
GET /car-route
POST /recommendations의 car_mode: auto | tmap | off
POST /recommendations의 max_car_route_calls: 기본 5
```

검증:

```text
자동 테스트 66개 통과
MVP 시나리오 10/10개 통과
TMAP 도보 실제 호출 성공
TMAP 자동차 실제 호출 성공
판단 신뢰도 최대 감점 15점 유지
```

## 2026-08-07 지도 선택 기반 여행 계획 입력

구현 순서:

```text
1. 여행 계획 JSON 규격 구현
2. TMAP 장소 통합검색 연결
```

사용자 직접 입력:

```text
여행 제목
여행 날짜
장소별 체류시간
시작시간과 예약시간은 선택 입력
```

지도 선택으로 자동 입력:

```text
TMAP 장소 ID
장소명
주소
경도·위도
카테고리
전화번호
```

장소 검색 실행:

```powershell
cd "C:\Users\hun\Documents\Codex\2026-07-28\files-mentioned-by-the-user-2026\outputs\plan_b_api"
.\장소검색_확인.ps1
```

JSON 검증 실행:

```powershell
.\여행계획_JSON_검증.ps1
```

웹 API:

```text
GET /place-search?q=경복궁&center_x=126.9767&center_y=37.5760&count=5
POST /trip-plans/validate
```

선택 버튼 처리:

```text
검색 결과 item 전체를 schedules[].place에 저장
사용자가 체류시간만 추가 입력
item_id는 프론트엔드에서 stop-1, stop-2 형식으로 생성
```

도보→버스 기준은 JSON에 15분으로 저장한다.
정확히 15분부터 버스로 전환하는 실행 로직은 지도 경로 구현 때 연결한다.

실제 TMAP 장소 검색은 현재 실행 환경의 외부 소켓 제한으로 보류됐다.
응답 파서·URL·인증 헤더는 모의 응답 테스트로 검증했다.

최종 검증:

```text
자동 테스트 71개 통과
MVP 시나리오 10/10개 통과
여행 계획 예제 JSON 검증 성공
로컬 HTTP 장소검색·계획검증 라우팅 성공
```

## 2026-08-07 경로 API 지도 좌표(GeoJSON) 저장

기존 경로 API는 시간·거리만 저장하고 실제 이동 경로 좌표는 버렸다.
지도 화면에 경로선을 그리려면 좌표가 필요하므로 세 경로 클라이언트에
`geometry` 필드를 추가했다.

```text
WalkingRoute.geometry   TMAP 보행 경로 응답의 LineString 조각을 이어붙인 좌표
CarRoute.geometry       TMAP 자동차 경로 응답의 LineString 조각을 이어붙인 좌표
SeoulTransitRoute.geometry  출발·도착 직선 좌표 (정류장 단위 좌표는 아직 미검증)
```

GeoJSON 형식:

```json
{
  "type": "LineString",
  "coordinates": [
    [126.9767, 37.5760],
    [126.9769, 37.5749]
  ]
}
```

추천 결과에도 후보별 경로 좌표를 추가했다.

```text
RankedTourCandidate.inbound_route_geometry  현재 위치 → 후보 경로
RankedTourCandidate.onward_route_geometry   후보 → 다음 고정 일정 경로
```

`POST /recommendations` 응답의 각 후보에 `route_geometry.inbound`,
`route_geometry.onward`가 추가된다.
`GET /walking-route`, `GET /car-route`, `GET /seoul-transit-route`,
CLI `walking-route`·`car-route`·`seoul-transit-route` 출력에도
`geometry`가 포함된다.

서울시 환승경로는 정류장별 좌표가 응답에 있는지 아직 실제 API로
확인하지 못했다. 현재는 항상 출발·도착 직선 좌표로 대체하며, 실제
정류장 좌표 스키마가 확인되면 정확한 다구간 경로로 교체해야 한다.

실제 API 검증:

```text
도보: 250m / 3.6분, 좌표 14개 (기존 시간·거리 값과 일치)
자동차: 4,489m / 26.22분, 좌표 201개
대중교통: SEOUL_TRANSIT_SERVICE_KEY 미등록으로 실 호출 실패
         (파서·직선 대체 로직은 모의 응답 테스트로 검증)
```

검증:

```text
자동 테스트 72개 통과
MVP 시나리오 10/10개 통과
```

## 2026-08-07 도보 15분 경계값 정책 확정

기존에는 도보 15분 이하(`<=15`)까지 도보를 유지했다.
정확히 15분일 때 도보로 남는지 버스로 넘어가는지 정책이 정해져
있지 않았다.

확정한 정책:

```text
도보시간 15분 미만  → 도보 유지
도보시간 15분 이상  → 자동차·대중교통으로 전환
```

변경 지점:

```text
signal_builder.py   within_walking_time: <= → <
optimized_recommender.py  다음 일정 구간 도보 판정 3곳: <= → <
```

영향 범위:

```text
현재 위치 → 후보 구간 도보 판정
후보 → 다음 고정 일정 구간 도보 판정
서울시 환승경로 내 도보 구간 판정
```

대중교통·자동차 30분 한도는 기존과 동일하게 `30분 이하`까지 통과한다.
이 정책은 그대로 유지한다.

검증:

```text
정확히 15.0분 도보: 필수조건 제외 확인
14.9분 도보: 정상 통과 확인
정확히 15분 도보 시 자동차 경로로 자동 전환 확인
자동 테스트 75개 통과
MVP 시나리오 10/10개 통과
```

## 2026-08-07 여행 계획 저장소·일정 교체·다음 일정 연결 함수

4단계(후보 선택 후 기존 일정 교체) API를 만들기 전에, 이를 지탱하는
백엔드 조각 3가지를 먼저 준비했다.

### 1. 저장소 (`trip_store.py`)

기존 `trip_plan.py`는 검증만 하고 저장하지 않는 stateless 구조였다.
`trip_id`로 여행 계획을 저장·조회하는 SQLite 저장소를 새로 만들었다.

```text
TripStore.create(plan) -> trip_id  (새 계획 저장, ID 발급)
TripStore.get(trip_id) -> TripPlan (저장된 계획 조회)
TripStore.save(trip_id, plan)      (기존 계획 덮어쓰기)
```

기존 `.cache/plan_b_api.sqlite3`와 별도 테이블(`trip_plans`)을 쓰며,
API 캐시와 달리 만료 없이 영구 보관한다. 없는 `trip_id`를 조회·저장하면
`TripNotFoundError`를 낸다.

### 2. 일정 교체 함수 (`trip_plan.replace_schedule_item`)

```text
입력: TripPlan, item_id, 새 place(SelectedPlace), 선택적 visit_minutes 등
동작: 해당 item_id 항목만 교체, 나머지 일정은 그대로 유지
출력: 교체된 TripPlan (원본은 변경하지 않음)
```

일정 ID를 찾지 못하면 오류를 낸다.

### 3. 다음 일정 연결 함수 (`trip_plan.next_schedule_item`,
`trip_plan.build_next_schedule_constraint`)

```text
next_schedule_item(plan, item_id)
  -> 바로 다음 일정 항목 (마지막 일정이면 None)

build_next_schedule_constraint(plan, next_item)
  -> 다음 일정에 fixed_arrival_time이 있으면 NextScheduleConstraint 생성
  -> 없으면 None (추천 엔진에 다음 일정 제약을 넘기지 않음)
```

`trip_date` + 다음 일정의 `fixed_arrival_time`을 합쳐 마감 시각
(`datetime`)을 만들고, 이미 있는 `schedule_feasibility.NextScheduleConstraint`
그대로 재사용한다. 즉 추천 엔진 쪽 코드는 수정하지 않았다.

아직 HTTP API로 연결하지 않았다. 이 세 조각을 엮어
"문제 일정 선택 → 추천 → 후보 선택 → 저장된 계획 교체" 엔드포인트를
만드는 게 다음 단계다.

검증:

```text
자동 테스트 85개 통과
MVP 시나리오 10/10개 통과
```

## 2026-08-07 여행 계획 저장·교체 HTTP API·CLI 연결

앞서 만든 저장소·교체 함수·연결 함수 세 조각을 실제 API로 엮었다.

### JSON API

```text
POST /trip-plans                        여행 계획 저장, trip_id 발급
GET  /trip-plans/{trip_id}               저장된 계획 조회
POST /trip-plans/{trip_id}/replace-schedule   일정 하나 교체 후 재저장
```

`replace-schedule` 요청 본문:

```json
{
  "item_id": "stop-1",
  "place": {
    "provider": "tmap",
    "place_id": "22019636",
    "name": "대한민국역사박물관",
    "longitude": 126.977,
    "latitude": 37.5738
  },
  "visit_minutes": 45
}
```

`visit_minutes`, `fixed_arrival_time`, `locked`은 선택 입력이며 생략하면
기존 값을 유지한다. 없는 `trip_id`로 조회·교체하면 HTTP 404를 반환한다.

### CLI

```powershell
python -X utf8 -m plan_b_api.cli create-trip-plan ".\examples\trip_plan_example.json"
python -X utf8 -m plan_b_api.cli get-trip-plan --trip-id <발급된 ID>
python -X utf8 -m plan_b_api.cli replace-trip-schedule --trip-id <ID> ".\examples\trip_schedule_replacement_example.json"
```

전체 흐름 확인:

```powershell
cd "C:\Users\hun\Documents\Codex\2026-07-28\files-mentioned-by-the-user-2026\outputs\plan_b_api"
.\여행계획_저장_교체_확인.ps1
```

한글이 포함된 PowerShell 문자열 리터럴은 인코딩 문제로 스크립트
파싱이 깨질 수 있어, 이 스크립트도 기존 스크립트들과 같이 안내문을
영문으로 쓰고 한글은 파이썬 CLI 출력에만 맡겼다.

저장소는 `.cache/plan_b_api.sqlite3`의 `trip_plans` 테이블을 그대로
쓰므로, 서버와 CLI가 같은 계획을 공유해서 조회·교체할 수 있다.

검증:

```text
create-trip-plan → get-trip-plan → replace-trip-schedule → get-trip-plan
실제 CLI·HTTP 양쪽 모두 정상 동작 확인
없는 trip_id 조회 시 오류 처리 확인 (CLI 오류 메시지, HTTP 404)
자동 테스트 86개 통과
MVP 시나리오 10/10개 통과
```

## 2026-08-08 - 웹 프런트엔드 추가 (web/index.html)

`server.py` JSON API를 직접 호출하는 정적 웹페이지를 추가했다.
빌드 도구 없이 HTML 한 파일(`web/index.html`)로 동작하며 지도는
Leaflet(CDN, OSM 타일)을 쓴다.

### 실행

```powershell
python -m plan_b_api.server --port 8000
```

이후 `web/index.html`을 브라우저로 직접 연다 (파일 열기, 별도 서버 불필요).
서버 CORS가 `*`로 열려 있어 `file://`에서 호출해도 동작한다.

### 화면 흐름

1. 지도에서 장소를 검색(`/place-search`)해 일정 목록에 순서대로 추가한다.
2. 각 일정 항목에 체류시간·고정 도착시각·잠금 여부를 지정한다.
3. "일정 저장" 클릭 시 `POST /trip-plans`로 저장하고, 발급된 `trip_id`를
   현재 URL에 `?trip=` 파라미터로 붙여 공유 링크를 만든다.
4. 다른 브라우저에서 그 링크를 열면 `GET /trip-plans/{id}`로 그대로
   이어서 편집할 수 있다.
5. 특정 일정 카드의 "이 일정 빼고 대체 추천" 클릭 시, 그 뒤에 있는
   가장 가까운 고정(fixed_arrival_time) 일정을 자동으로 찾아
   `next_longitude/next_latitude/next_arrival_time` 제약으로 변환해
   `POST /recommendations`를 호출한다 — 다음 고정 일정 시각을
   못 맞추는 후보는 추천에서 자동 제외된다.
6. 추천 후보 선택 시 `POST /trip-plans/{id}/replace-schedule`로 그
   자리만 교체하고 나머지 일정은 그대로 둔다.
7. 일정 항목 사이 구간은 도보(`/walking-route`)·차량(`/car-route`)·
   대중교통(`/seoul-transit-route`) 중 골라 실제 경로를 지도에 표시한다.

### 발견·수정한 버그

`api_service.py`의 우선순위 기본값이 `("weather_fit", "route_time",
"budget_fit")`였는데, `scoring.py`의 `PRIORITY_FIELDS`에는
`budget_fit`이 없어(5개 필드: weather_fit, route_time,
crowd_avoidance, child_fit, walking_fit) **우선순위를 하나도 지정하지
않고 `/recommendations`를 호출하면 항상 실패**했다.
기본값을 `("weather_fit", "route_time", "crowd_avoidance")`로
고쳤다.

### 검증

```text
curl로 /place-search, POST /trip-plans, GET /trip-plans/{id},
POST /recommendations(다음 고정일정 제약 포함),
POST /trip-plans/{id}/replace-schedule 순서로 실제 호출해
프런트엔드가 만드는 JSON 형식과 백엔드 응답이 그대로 맞물리는지 확인.
node --check로 index.html에 내장된 JS 문법 오류 없음 확인.
브라우저 확장 미설치로 실제 클릭 조작 테스트는 못 했음 — 사용자가
직접 열어서 확인 필요.
```

## 2026-08-08 - 대중교통 경로 조회를 ODsay로 교체

공공데이터포털 `서울특별시_대중교통환승경로 조회 서비스`(15000414)를
활용신청·연장까지 했는데도 실제 `ws.bus.go.kr` 서버가 계속
`SERVICE_KEY_IS_NOT_REGISTERED_ERROR`를 반환했다. 조사 결과 TOPIS
공식 안내에 "Open API는 서울 열린데이터광장(data.seoul.go.kr)을
통해서만 신청 가능"이라고 명시돼 있어, 공공데이터포털 발급 키로는
애초에 `ws.bus.go.kr`을 호출할 수 없는 구조로 보인다. 서울 열린
데이터광장에서도 정확한 상품명을 찾지 못해, 국내 대중교통 길찾기
표준으로 많이 쓰이는 [ODsay](https://lab.odsay.com) API로 교체했다.

`seoul_transit_client.py`의 클래스·함수 이름(`SeoulTransitClient`,
`SeoulTransitRoute`, `SeoulTransitApiError`,
`parse_seoul_transit_response`)은 그대로 유지하고 내부 구현만
`api.odsay.com/v1/api/searchPubTransPathT` 호출로 교체했다. 환경변수는
`SEOUL_TRANSIT_SERVICE_KEY` → `ODSAY_API_KEY`로 바뀌었고, 더 이상
`KTO_SERVICE_KEY`로 자동 대체하지 않는다(서로 다른 발급기관 키라
자동 재사용이 애초에 잘못된 설계였다).

ODsay 응답에서 정류장 좌표(`subPath[].passStopList.stations`)를 모아
실제 경로 geometry를 만들고, 지하철 호선명·버스 노선명
(`subPath[].lane.name`)을 이어붙여 `route_type`에 넣도록 개선했다
(예: "272 → 수도권 2호선"). 기존에는 이 정보가 전혀 없었다.

### 검증

```text
자동 테스트 87개 통과 (test_seoul_transit_client.py를 ODsay 응답
형식에 맞게 재작성, 에러 응답 테스트 추가).
```

ODsay API 키 발급(서버 환경, 공인 IP 등록) 후 실제 라이브 호출로
재검증 완료. 경복궁→DDP 실제 결과: 24분/4.6km, 도보 10분/675m,
환승 2회, 경로 "01A → 수도권 2호선" — 버스 노선번호·지하철
호선명까지 정상 표시됨.

## 2026-08-08 - 도보 경로 turn-by-turn 안내 추가

TMAP 보행자 경로 API 응답의 Point 피처마다 `description`에 이미
완성된 한국어 안내문("좌회전 후 보행자도로를 따라 34m 이동")이
들어있다는 걸 확인했다 (터미널에 출력했을 때 인코딩이 깨져 보여서
처음엔 API 자체 버그인 줄 알았는데, 파일로 저장해서 확인하니
데이터 자체는 멀쩡했다 — 터미널 출력 문제였음).

`route_client.py`에 `WalkingStep`(instruction, turn_type,
distance_meters, longitude, latitude) 데이터클래스를 추가하고,
Point 피처와 그 다음 LineString 피처의 거리를 묶어서
`WalkingRoute.steps`로 구성한다. `/walking-route` 응답에도
`steps` 배열을 포함시켰다.

웹 화면에서는 도보 경로를 조회하면 지도 오른쪽에 `#directions-panel`이
열리면서 안내문 목록(문구 + 남은 거리)을 순서대로 보여주고, 각
꺾이는 지점마다 지도에 작은 점 마커를 찍는다.

### 검증

```text
자동 테스트 88개 통과 (test_route_client.py에 turn-by-turn 파싱
테스트 추가).
/walking-route HTTP 호출로 실제 한글 안내문이 깨지지 않고 그대로
오는 것을 확인.
브라우저 클릭 테스트는 못 했음 — 사용자가 도보 경로 조회 버튼을
직접 눌러서 오른쪽 패널 확인 필요.
```

## 2026-08-08 - 대중교통 경로에 승하차·환승·출입구 상세 추가

ODsay 응답의 subPath 배열이 이미 구간별 상세 정보를 담고 있었다.
공식 API 레퍼런스를 확인해 다음 필드를 활용했다.

- `startName`/`endName`: 승차·하차 정류장/역명
- `startExitNo`/`endExitNo`: 지하철 진입/하차 후 출구 번호
- `lane.name`/`lane.busNo`: 호선명 또는 버스 번호
- `stationCount`: 경유 정거장 수
- 도보 구간(trafficType=3)도 같은 subPath 배열 안에 섞여 있어서
  대중교통 전후·환승 사이 도보까지 자연스럽게 포함된다.

`seoul_transit_client.py`에 `SeoulTransitLeg` 데이터클래스를 추가해
구간마다 "을지로3가 12번 입구로 진입 · 수도권 2호선 승차 →
동대문역사문화공원에서 하차 (2개 정거장 이동) · 1번 출구로 이동"
같은 한 줄 안내문을 만든다. `SeoulTransitRoute.legs`에 순서대로
담기며, `/seoul-transit-route` 응답에도 그대로 포함된다.

웹 화면의 오른쪽 `#directions-panel`을 도보 전용에서 범용으로
바꿔서, 대중교통 조회 시에도 같은 패널에 도보(🚶)·버스(🚌)·
지하철(🚇) 구간을 순서대로 보여준다.

### 검증

```text
자동 테스트 89개 통과 (환승·출입구 번호 포함 케이스 테스트 추가).
실제 ODsay 키로 경복궁→DDP 라이브 호출 확인:
도보 229m → 버스 01A(경복궁.국립민속박물관→명동성당.남대문세무서,
4개 정류장) → 도보 341m → 지하철 수도권 2호선(을지로3가 12번
입구 진입→동대문역사문화공원, 2개 정거장, 1번 출구로 이동) →
도보 105m. 승하차 정류장명·출입구 번호·환승 도보까지 전부 정상.
캐시에 남아있던 예전 응답(legs 없음)이 처음엔 잡혀서 빈 배열이
나왔음 — api_cache 테이블에서 seoul-transit: 키만 지우고 재검증함
(trip_plans 테이블은 그대로 둠).
```

## 2026-08-08 - 대중교통 지도 경로선을 TMAP 실제 도보 경로로 보강

ODsay의 도보 구간(trafficType=3)은 distance/sectionTime만 주고
좌표를 전혀 주지 않는다는 걸 확인했다(공식 레퍼런스는 모든
구간에 좌표가 있다고 돼 있었지만 실제 응답은 달랐다). 그래서
지도에 그리던 전체 경로선이 도보 구간마다 뚝뚝 끊기거나
정류장·역만 직선으로 잇는 식이라 부정확했다.

`seoul_transit_client.py`에 두 함수를 추가했다.

- `fill_walk_leg_endpoints`: 도보 구간의 시작/끝 좌표를 바로
  앞뒤 구간(지하철·버스는 좌표가 있음)에서 채운다. 첫 도보 구간은
  전체 출발지, 마지막 도보 구간은 전체 도착지를 쓴다.
- `combine_leg_geometries`: 구간별 geometry를 순서대로 이어붙여
  하나의 선으로 만든다.

`optimized_recommender.py`에 `enrich_seoul_transit_walk_geometry`를
추가해 도보 구간마다 `cached_walking_route`(TMAP)를 호출해 실제
보행 경로로 교체한다. 추천 엔진 내부(다음 일정 도착 가능성 계산
등)에는 지도가 필요 없으므로 적용하지 않고, `/seoul-transit-route`
HTTP 엔드포인트에서만 적용해 불필요한 TMAP 호출을 늘리지 않았다.
TMAP 키가 없으면 조용히 건너뛰고 기존 직선 근사를 유지한다.

결과적으로 "장소 → 첫 정류장/역 입구"와 "마지막 정류장/역 출구 →
목적지" 구간도 실제 도보 경로로 그려진다.

### 검증

```text
자동 테스트 91개 통과 (도보 좌표 채우기·경로 이어붙이기·TMAP
보강 각각 테스트 추가). 테스트 작성 중 실제로 버그 하나 잡음 —
바로 옆 구간에 좌표가 없을 때 엉뚱한 좌표로 채워지는 경우가
있어서 테스트 픽스처를 실제 ODsay 응답 형태(좌표 포함)로 맞춤.

실제 ODsay+TMAP 키로 경복궁→DDP 라이브 호출 확인: 전체 경로선이
8개 점(정류장만)에서 76개 점(실제 도보 곡선 포함)으로 늘어남,
API 호출 4회(ODsay 1 + TMAP 도보 3). 캐시된 예전 응답이 남아있어
처음엔 반영 안 됐음 — api_cache에서 seoul-transit: 키만 다시
지우고 재검증함.
```

## 2026-08-08 - 지도 타일을 CARTO Voyager로 교체, 지하철 입구·버스 승차 마커 추가

### 지도 타일

기존 OSM 기본 타일은 아이콘·라벨이 빽빽해서 길이 잘 안 보인다는
피드백이 있었다. CARTO Voyager(무료, 키 불필요)로 교체했다 —
내비게이션용으로 설계된 스타일이라 도로 위계가 색·굵기로
뚜렷하게 구분된다. `web/index.html`의 타일 URL만 교체하면 됐다.

### 지하철 입구·버스 승차 위치 마커

ODsay 응답에 `startExitX/Y`, `endExitX/Y`(지하철 출입구 정확한
좌표)가 있다는 걸 확인했다. 역 중심 좌표와는 별개 값이라 더
정확하다(예: 을지로3가역 중심과 12번 입구는 좌표가 다름).
`SeoulTransitLeg`에 `start_entrance_longitude/latitude`,
`end_exit_longitude/latitude` 필드를 추가해 담았다. 값이 0이면
"출입구 정보 없음"이라는 ODsay 관례라 좌표로 취급하지 않는다.

웹 화면에서는 대중교통 경로 조회 시 지도에:
- 버스 구간: 승차·하차 정류장에 🚌 마커
- 지하철 구간: (있으면 출입구 정확좌표로) 입구·출구에 🚇 마커

를 각각 찍는다. 클릭하면 "OO역 12번 입구" 같은 팝업이 뜬다.

### 검증

```text
자동 테스트 91개 통과 (기존 테스트 그대로 통과, 필드 추가는
하위 호환).
실제 ODsay 응답으로 확인: 을지로3가역 중심좌표(126.991053,
37.566285)와 12번 입구 좌표(126.989382, 37.566019)가 실제로
다르게 나옴 — 정확한 입구 위치 반영 확인.
CARTO Voyager 타일 URL 응답 200 확인.
```

## 2026-08-08 - 구간별 지도 색상, 대중교통 도보 turn-by-turn, 아이콘 토글

### 일정 구간별 색상

지도 위 1→2, 2→3 ... 각 구간 선을 8색 팔레트로 순환 배정했다
(`LEG_COLORS`). 일정 목록의 구간 줄에도 같은 색 네모를 붙여서
지도 선과 목록 줄을 바로 대응시켰다.

### 대중교통 안의 도보 구간도 turn-by-turn으로

`enrich_seoul_transit_walk_geometry`가 이미 TMAP으로 도보 구간의
실제 경로를 가져오고 있었는데, 그 응답에 이미 들어있던
`WalkingRoute.steps`(턴바이턴 안내)를 안 쓰고 버리고 있었다.
`SeoulTransitLeg`에 `steps` 필드를 추가해 그대로 담았다.

웹 화면에서는 대중교통 조회 시 도보 구간을 만나면 그 구간을
"도보로 341m 이동" 한 줄 대신, 도보 단독 조회와 똑같이
꺾이는 지점마다 개별 줄("좌회전 후 삼청로를 따라 19m 이동" 등)로
풀어서 보여준다(`leg.steps`가 있으면 펼치고, 없으면 기존처럼
한 줄 요약).

### 아이콘 켜고 끄기 토글

경로 상세 패널 상단에 "아이콘 표시" 체크박스를 추가했다. 끄면
패널의 이모지와 지도 위 도보/버스/지하철 핀이 전부 작은 점
마커로 바뀐다. 새로 API를 호출하지 않고 마지막으로 그렸던
데이터(`state.lastDirections`)를 그대로 재렌더링한다.

### 검증

```text
자동 테스트 91개 통과 (TMAP 도보 클라이언트 fake에 steps 추가,
enrich 함수가 steps까지 옮기는지 검증하는 assertion 추가).
실제 ODsay+TMAP 라이브 호출로 확인: 도보 구간마다 "좌회전 후
보행자도로를 따라 14m 이동" 같은 턴바이턴 문장이 그대로 옴
(기존 독립 도보 조회와 동일한 수준).
node --check로 JS 문법 확인.
```

## 2026-08-08 - 도보 화살표 실제 방위각 회전, 구간별 지도 표시 on/off

### 화살표가 텍스트가 아니라 실제 방향을 가리키게

기존엔 turnType(좌회전=12, 우회전=13 등) 값에 고정된 이모지
(⬅️➡️)를 썼는데, 이건 "왼쪽/오른쪽"이라는 대략적인 분류일 뿐
지도 위 실제 진행 방향과는 무관했다. 프론트엔드에서 현재
지점과 다음 지점의 좌표로 방위각(bearing)을 직접 계산해서
"⬆️" 화살표를 그 각도만큼 회전시키는 방식으로 바꿨다
(`bearingDegrees()`, CSS `transform: rotate()`). 출발(🚩)·
도착(🏁) 지점은 회전 대상이 아니라 그대로 둔다. 백엔드 변경
없이 프론트엔드 계산만으로 해결됨.

### 구간별 지도 표시 켜고 끄기

일정 목록의 각 구간 줄에 체크박스를 추가했다. 끄면 그 구간의
색깔 선만 지도에서 사라지고(`leg.visible = false`), 다른 구간엔
영향 없다. 새 API 호출 없이 `redrawScheduleLayer()`만 다시 실행한다.

### 검증

```text
node --check로 JS 문법 확인.
브라우저 클릭 테스트는 못 했음 — 화살표 회전 각도와 체크박스
on/off 동작을 사용자가 직접 확인 필요.
```

## 2026-08-08 - 일정 전체 저장 API + 낙관적 동시성 제어

다른 LLM(퍼플렉시티)에 프로젝트 현황을 설명하고 다음 단계를
물어본 리포트를 받았다. 지적한 것 중 실제로 맞았던 부분: 웹
화면의 "일정 저장" 버튼이 매번 `POST /trip-plans`(새로 생성)만
호출해서, 순서를 바꾸거나 항목을 편집한 뒤 저장하면 **기존
trip_id를 버리고 매번 새 링크가 생기는** 실사용 버그가 있었다
(공유해둔 링크가 조용히 무효화됨). 이걸 고치면서 버전 관리도
같이 넣었다.

`trip_store.py`:
- `trip_plans` 테이블에 `version` 컬럼 추가. 이미 있던 실제
  데이터(예진에게 공유했던 `2a0bc7dd0aff` 포함, 총 8개 여행)에는
  서버 시작 시 자동 마이그레이션(`ALTER TABLE ... ADD COLUMN`)으로
  전부 `version=1`이 부여됨 — 기존 데이터 그대로 보존됨.
- `save()`가 `expected_version`을 받아 `UPDATE ... WHERE trip_id=?
  AND version=?`로 비교·교체(compare-and-swap)한다. 버전이
  다르면 새 예외 `TripVersionConflictError`를 낸다.
- `get()`은 이제 `(plan, version)` 튜플을 반환한다.

`api_service.py`에 `replace_trip_plan()` 추가 — 일정 전체를
통째로 덮어쓰는 로직, `expected_version` 필수.

`server.py`에 `PUT /trip-plans/{id}` 라우트 추가
(`do_PUT` 신설, JSON 파싱 로직은 `_read_json_body()`로 뽑아서
POST와 공유). 버전 충돌 시 HTTP 409 반환.

웹 화면: `state.tripVersion` 추적. 저장 버튼이 이제 `tripId`가
있으면 `PUT`(버전 포함)으로 덮어쓰고, 없으면 `POST`로 새로
만든다. 409 받으면 "다른 곳에서 먼저 저장됨" 토스트 띄우고
최신 내용을 자동으로 다시 불러온다(`loadTripById()`로 통합).

### 검증

```text
자동 테스트 94개 통과 (trip_store 버전 충돌 테스트, 서버
PUT/409 통합 테스트 추가).
실제 .cache/plan_b_api.sqlite3(예진 공유분 포함 실데이터)에
마이그레이션 직접 실행해 확인 — 기존 8개 여행 전부 보존되고
version=1 부여됨, 라이브 서버로 GET해서 version 필드 확인.
node --check로 JS 문법 확인.
```

## 2026-08-08 - 대체 추천에 카테고리 유사도 반영

퍼플렉시티 리포트가 지적한 5순위 항목("에버랜드 대신 롯데월드
같은 비슷한 카테고리") 착수. KTO `locationBasedList2`는
`contentTypeId`만 필터링 가능하고 `cat1/cat2/cat3`(대/중/소
분류) 필터는 안 되지만, 응답 raw item에는 `cat1/cat2/cat3` 값이
이미 들어있다는 걸 확인했다. 그래서 서버가 필터링해주는 대신,
이미 가져온 후보들을 우리 쪽에서 재정렬하는 방식으로 구현했다.

- `RankedTourCandidate`에 `cat1/cat2/cat3` 필드 추가, 원본 KTO
  item에서 그대로 담는다.
- `recommend_nearby_optimized()`에 `preferred_cat2` 파라미터
  추가. 최종 정렬을 `(카테고리 일치 여부, 점수)` 튜플 기준으로
  바꿔서, 일치하는 후보가 있으면 거리·점수와 무관하게 맨 위로
  온다. 일치하는 게 하나도 없으면 원래 순서(점수순) 그대로라
  결과가 비는 일은 없다. **탐색 반경이나 API 호출량은 그대로**
  라 "이미 찾은 후보 중에서 비슷한 게 있으면 우선한다"는 수준의
  개선이다(에버랜드처럼 멀리 있는 유사 카테고리까지 새로 찾아오는
  건 아님).
- `SelectedPlace`(trip_plan.py)에도 `cat1/cat2/cat3` 저장 —
  KTO 추천으로 담긴 장소는 카테고리를 기억했다가 나중에 그
  장소를 뺄 때 다시 쓸 수 있다. TMap 검색으로 직접 추가한 장소는
  카테고리 정보가 없다(KTO 분류 체계가 아니므로).
- 웹 화면: "이 일정 빼고 대체 추천" 패널에 "비슷한 카테고리
  우선" 체크박스 추가. 뺄 장소에 카테고리 정보가 없으면
  비활성화되고 이유가 표시된다. 일치하는 후보 카드에는 "비슷한
  카테고리" 표시가 붙는다.

### 검증

```text
자동 테스트 97개 통과 (카테고리 우선순위 재정렬 테스트 3개 추가:
선호 없음/일치 있음/일치 없음 각각).
실제 KTO API로 에버랜드 인근 좌표 호출 확인: cat2=A0302(레저
스포츠류) 두 후보가 기본 정렬에서는 3·4위였는데
preferred_cat2=A0302로 재요청하니 1·2위로 올라옴 — 나머지
카테고리 불일치 후보는 그대로 점수순 유지.
node --check로 JS 문법 확인.
```

## 2026-08-08 - CLI 대중교통 일관성 + 브라우저 자동 테스트

### CLI 대중교통 일관성

`seoul-transit-route` CLI 명령이 웹 API와 달리 도보 구간 보강
(`enrich_seoul_transit_walk_geometry`)을 안 거치고 있었다.
같은 로직을 CLI에도 적용해서 웹과 동일하게 turn-by-turn·실제
경로선이 나오게 맞췄다. 라이브 확인: 전체 좌표점 67개, 도보
구간마다 실제 경로+안내문 포함.

### 브라우저 자동 테스트 추가 (`tests_e2e/`)

처음으로 외부 패키지(Playwright)를 도입했다 — 단, 앱 실행에는
필요 없고 `tests_e2e/`에서만 쓰는 선택적 의존성으로 분리했다
(`pyproject.toml`의 `[project.optional-dependencies].e2e`).

`tests/`(단위 테스트, 외부 의존성 없음)와 분리해서
`tests_e2e/test_web_ui.py`에 실제 Chromium + 실제 API 키로 8개
핵심 시나리오를 검증한다: 연결상태, 검색→추가, 잠금 시 버튼
비활성화, 삭제, 드래그 순서변경, 지도 표시 토글, 순서변경 시
구간경로 초기화, 저장→공유링크→재접속 시 복원.

드래그 테스트에서 실제로 막힌 부분: Playwright의
`locator.drag_to()`(마우스 다운·이동·업 합성)로는 Chromium의
네이티브 HTML5 `dragstart`가 안 걸렸다(알려진 제약 — 합성 마우스
이벤트만으론 OS 레벨 드래그 제스처가 인식 안 됨). `page.evaluate`로
`DragEvent`를 직접 dispatch(`dragstart`→`dragover`→`drop`→
`dragend`, 공유 `DataTransfer`)하는 방식으로 우회해서 해결했다.

### 검증

```text
단위 테스트 97개 그대로 통과(회귀 없음).
브라우저 테스트 8개 전부 통과 — Playwright 1.62 + Chromium
헤드리스, 실제 .env 키로 실행.
```

## 2026-08-08 - 모바일 반응형 레이아웃 (목록/지도 탭)

Playwright로 390px(일반 스마트폰) 뷰포트에서 진단해보니 완전히
깨져있었다 — 지도 폭이 0px(안 보임), 가로 스크롤 76px 발생.
사이드바가 460px 고정폭이라 좁은 화면에서 flex 계산상 지도를
전부 밀어냈기 때문이다.

파일을 두 개(데스크톱/모바일)로 나누는 대신 `web/index.html`
하나에 CSS 미디어 쿼리(`max-width: 768px`)로 자동 전환되게
만들었다 — 데스크톱은 그대로, 좁은 화면에서만 아래 레이아웃이
켜진다.

- `body`에 `data-mobile-tab="list"|"map"` 속성을 두고, 상단에
  "목록"/"지도" 탭 버튼 2개를 추가했다. 좁은 화면에서는 둘 중
  하나만 전체 화면으로 보인다.
- 사이드바 리사이저는 숨긴다(터치 환경에서 의미 없음).
- 리사이저 JS가 남긴 인라인 `width`를 모바일에서 무시하도록
  `!important`로 덮었다.
- 경로 상세 패널(`#directions-panel`)은 모바일에서 하단에서
  올라오는 바텀시트(고정 위치, 화면의 60% 높이)로 바뀐다.
- 지도가 `display:none` 상태에서 보이는 상태로 바뀔 때
  Leaflet이 크기를 다시 계산하도록 탭 전환 시
  `map.invalidateSize()`를 호출한다.

### 검증

```text
Playwright로 재진단: 목록 탭 가로 스크롤 0px, 지도 탭 전환 시
지도 폭 390px·높이 796px 정상 렌더링, 가로 스크롤 0px.
스크린샷으로 실제 렌더링 확인(버튼 잘림 없음, 지도 타일 정상).
tests_e2e에 회귀 테스트 추가 — 전체 9개 통과.
단위 테스트 97개 그대로 통과.
```

## 2026-08-08 - 순서변경 시 경로 자동 재계산, 지하철 실제 호선 색상

다른 지도 앱 재조사에서 나온 아이디어 2개 적용.

### 순서 바꾸면 즉시 자동 재계산 (티맵 벤치마킹)

지금까지는 드래그로 순서를 바꾸면 구간 경로가 전부 "미조회"로
초기화되고 사용자가 도보/차량/대중교통 버튼을 일일이 다시 눌러야
했다. 티맵은 경유지가 바뀌면 즉시 전체 경로를 재계산해서
보여준다는 걸 참고해서, `state.lastRouteMode`(마지막으로 쓴
이동수단)를 기억해뒀다가 순서 변경 직후 그 수단으로 모든 인접
구간을 자동으로 다시 조회하게 만들었다. 한 번도 경로를 조회한
적 없으면(=기준 수단이 없으면) 예전처럼 조용히 초기화만 한다.

### 지하철 구간을 실제 호선 색으로 표시 (카카오지하철 벤치마킹)

대중교통 조회 결과의 `legs` 배열(도보/버스/지하철 구간별 정보와
각자의 geometry)을 지도에 그릴 때, 이제 구간 전체를 한 색으로
뭉뚱그리지 않고 종류별로 다르게 그린다: 도보는 회색 점선, 버스는
기존 팔레트색, 지하철은 노선명(`lane_name`)에서 "2호선", "5호선"
같은 키워드를 찾아 서울 지하철 공식 노선색(2호선 초록 `#00A84D`
등)을 매칭해서 그린다. 매칭 안 되는 노선명은 회색으로 대체한다.

### 검증

```text
실제 도보 경로로 자동 재계산 라이브 확인: 일정 3개 만들고 순서
바꾸니 새로 생긴 두 인접 구간에 대해 walking-route API가 자동
호출되고 결과가 바로 표시됨(수동 클릭 없이).

지하철 색상은 ODsay 키가 마침 IP 인증 실패 상태라 실제 API로는
확인 못 하고(등록된 IP 180.182.66.237 ≠ 현재 IP
165.229.140.137 — 유동 IP로 바뀜, 재등록 필요), 브라우저에
모의 legs 데이터를 주입해 렌더링만 검증함: 도보 #9aa3af,
지하철(수도권 2호선) #00A84D, 버스 #2d6cdf로 정확히 구분되어
그려짐.

tests_e2e 회귀 테스트 갱신(reorder 테스트가 이제 "미조회"가 아니라
자동 재계산된 결과를 검증하도록 수정) — 9개 전부 통과.
단위 테스트 97개 그대로 통과.
```

## 2026-08-08 - 사이드바 상단 "연결 상태" 섹션 제거

개발 초기 진단용으로 넣어뒀던 API 서버 주소 입력창·연결확인
버튼·tour_api 등 체크리스트를 화면에서 없앴다. `api-base`
입력값 자체(모든 API 호출이 참조하는 기준 URL)는 기능이 필요해서
`<input type="hidden">`로 남기고, 페이지 로드 시 같은 origin으로
자동 설정하는 로직은 그대로 유지했다. `checkHealth()` 함수와
관련 CSS(`#status-dot`)는 삭제했다.

### 검증

```text
사이드바가 "여행 정보"부터 바로 시작하는 것 스크린샷으로 확인.
tests_e2e의 status-dot 대기 로직을 /priorities 응답(접힌
<details> 안에 있어도 DOM에는 붙어있는 우선순위 칩) 대기로
교체 — 관련 없어진 테스트(health check) 삭제, 나머지 8개 통과.
단위 테스트 97개 그대로 통과.
```

## 2026-08-08 - 직접 검색으로 넣은 장소도 카테고리 매칭

"비슷한 카테고리 우선" 체크박스는 cat2(관광공사 중분류)가 있는
장소만 켜졌다. TMAP "장소 검색"으로 넣은 장소는 관광공사 분류
체계 자체가 없어서 항상 비활성화였는데, 이제 일정에 추가하는
순간 관광공사 데이터에서 같은 장소를 찾아 cat1/cat2/cat3를
보강한다.

### 매칭 방식

```text
GET /kto-category-match?x=경도&y=위도&name=장소명

1. 관광공사 키워드검색(searchKeyword2)으로 이름이 같은 후보를 모은다
   (이름은 정확히 찾지만 cat1/cat2/cat3가 비어 있음)
2. 관광공사 위치기반검색(locationBasedList2)으로 반경 안 후보도 모은다
   (cat1/cat2/cat3는 채워져 있지만 위치 색인이 실제 좌표와
   어긋난 콘텐츠가 있어 반경 안에 없을 수 있음)
3. 두 결과를 합쳐 이름 일치도 → 거리 순으로 가장 그럴듯한 하나를 선택
   (기본 반경 1,500m, 같은 이름의 먼 지역 장소는 자동 제외됨)
```

프런트엔드는 검색으로 장소를 추가할 때(`addToSchedule`) cat2가
없으면 이 API를 자동으로 호출해서 채운다. 실패해도 장소 추가
자체는 그대로 진행된다.

### 알려진 한계

관광공사 콘텐츠 자체에 cat1/cat2/cat3가 비어 있는 경우도 실제로
있다 (예: 경복궁 원본 콘텐츠, contentid 126508). 이때는 관광공사
데이터와 정확히 매칭되어도(`matched: true`) 카테고리는 여전히
빈 값이며, 이건 매칭 로직 문제가 아니라 관광공사 원본 데이터의
누락이다.

### 실제 검증

```text
경복궁 → 매칭 성공(contentid 126508), 다만 cat1/cat2/cat3 원본 자체가 비어 있음
대한민국역사박물관 → 매칭 성공(contentid 1849416), cat2=A0206(문화시설)
```

### 부수 발견: 이전 UI 개편으로 깨져 있던 E2E 테스트 수정

`tests_e2e/test_web_ui.py`(Playwright 브라우저 테스트, 별도 실행)를
돌려보니 최근 두 차례 UI 개편(모바일 바텀시트 전환, 우측 패널
일정/경로상세 탭 분리)이 반영 안 된 테스트 3개가 실패하고 있었다.

```text
1. 모바일 목록/지도 탭 테스트 → 이제 지도는 항상 배경이고 목록은
   바텀시트이므로, 새 구조(#map 상시 표시, #sidebar-handle·
   #directions-handle 존재)를 확인하도록 재작성
2·3. 구간 "지도에 표시" 체크박스 관련 테스트 2개 → 경로 조회 시
   "경로 상세" 탭으로 자동 전환되면서 "일정" 탭 안의 체크박스가
   가려져 타임아웃남. "일정" 탭으로 돌아온 뒤 상호작용하도록 수정,
   텍스트 확인은 탭 상태와 무관하게 textContent로 직접 확인하도록 변경
```

실행:

```powershell
pip install playwright
python -m playwright install chromium
python -m unittest discover -s tests_e2e -v
```

검증:

```text
E2E 테스트 8개 전부 통과 (수정 전 3개 실패)
단위 테스트 98개 통과
```

## 2026-08-08 - 대체 추천 개선 3종

### 1. 추천할 때 자기 자신 제외

지워질 장소 근처를 검색하면 그 장소 자신이 후보로 다시 나오는
문제가 있었다. 두 가지 방법을 같이 적용한다.

```text
exclude_longitude/exclude_latitude: 지워질 장소 좌표 반경 50m 이내 후보 제외
exclude_content_id: 지워질 장소의 관광공사 콘텐츠 ID와 같으면 제외
```

거리 기준만으로는 부족한 경우가 실제로 있었다 — TMAP 좌표와
관광공사 등록 좌표가 같은 장소인데도 200m 넘게 차이 나는 경우를
확인했다(국립중앙박물관: TMAP 126.9781/37.5232, 관광공사
126.9791/37.5211, 거리 약 248m). 그래서 콘텐츠 ID 기준도 함께
쓴다. 콘텐츠 ID는 검색으로 추가할 때 카테고리 매칭과 함께
저장해두고(`kto_content_id`), 대체 추천 요청 시 항상 같이
보낸다.

### 2. cat1/cat2/cat3가 없는 콘텐츠의 카테고리 매칭

국립중앙박물관처럼 관광공사 구(舊) 분류체계(cat1/cat2/cat3)가
비어 있는 콘텐츠가 실제로 있다. 관광공사가 이후 도입한 신(新)
분류체계 `lclsSystm1/2/3`은 이런 콘텐츠에도 채워져 있는 경우가
많다는 걸 확인했다(국립중앙박물관: cat1/2/3 전부 빈값,
lclsSystm1=VE, lclsSystm2=VE07, lclsSystm3=VE070100).

```text
cat2가 있으면 cat2로 매칭(기존과 동일)
cat2가 없고 lclsSystm2(줄여서 lcls2)가 있으면 lcls2로 매칭
둘 다 없으면 여전히 "비슷한 카테고리 우선" 사용 불가
```

"비슷한 카테고리 우선" 정렬 로직도 cat2 일치 OR lcls2 일치면
우선하도록 바꿨다. `SelectedPlace`, `RankedTourCandidate`,
`/kto-category-match`, `POST /recommendations` 모두 lcls1/2/3을
추가로 주고받는다.

### 3. 추천 후보에 점수 breakdown 대신 장소 설명 표시

기존에는 "날씨 적합 99% · 보행 부담 97% · 이동시간 96%" 같은
점수 구성요소를 보여줬는데, 이제 해당 항목 대신 관광공사
상세정보의 개요(overview)를 90자로 잘라서 보여준다. 점수·주소·
이동시간·혼잡도는 그대로 유지.

### 실제 검증 (Playwright로 실제 화면 조작)

```text
국립중앙박물관 검색 → 일정에 추가 → 카테고리 매칭 자동 시도
→ cat2는 비어 있고 lcls2=VE07로 매칭됨 → 체크박스 활성화 확인
→ 저장 → 대체 추천 → "비슷한 카테고리 우선" 체크 → 추천 받기

실제로 서버에 전송된 요청 본문:
exclude_longitude/exclude_latitude: 국립중앙박물관 TMAP 좌표
exclude_content_id: "129703" (국립중앙박물관 관광공사 콘텐츠 ID)
preferred_lcls2: "VE07"

결과 1위: 국립중앙박물관 전통염료식물원 — 설명 문구가 점수
breakdown 대신 표시됨 확인
```

검증:

```text
단위 테스트 98개 통과
E2E 테스트 8개 통과
```

## 2026-08-08 - 검색 결과 카테고리·거리 강조 (동명이인 방지)

"에버랜드" 검색 시 실제 용인 놀이공원이 아니라 서울 용산의
동명 IT 매장이 1위로 잘못 클릭된 사례를 실제로 재현해서 확인했다
(지도 중심이 이전에 추가한 서울 지역 일정 쪽으로 옮겨진 상태에서
검색하면, TMAP이 거리 우선으로 동명 결과를 앞세움).

검색 결과 카드를 다음처럼 바꿨다.

```text
기존: 제목 아래 작은 회색 글씨로 "주소 · 카테고리"
변경: 제목 옆에 카테고리를 파란 배지로 바로 표시
     + "현재 지도 중심에서 약 X km" 거리 안내 추가
```

실제 확인:

```text
에버랜드(서울 용산, IT H/W) 배지가 "IT H/W"로 바로 눈에 띄어
테마파크를 기대하는 사용자가 한눈에 다른 곳임을 알아챌 수 있음
거리도 "약 1.5km"로 표시되어 실제 용인(40km 이상)과 다름을 알 수 있음
```

검증: E2E 테스트 8개 그대로 통과 (백엔드 변경 없음, 프런트만 수정).

## 2026-08-08 - 지도 중심 편향 검색이 유명 장소를 밀어내는 문제

"에버랜드를 검색해도 진짜 에버랜드가 안 나온다"는 문제를 재현해서
원인을 확인했다.

### 원인

`/place-search`는 항상 현재 지도 중심 좌표를 TMAP에 같이 보낸다
(`centerLon`/`centerLat`/`radius=20km`, 일정에 서울 지역 장소를
먼저 추가하면 지도가 그쪽으로 이동한다). TMAP은 중심 좌표가 주어지면
**이름 정확도보다 거리를 우선**해서 정렬한다. 그래서 진짜 에버랜드
(용인, 약 32km 거리)보다 이름만 같은 서울의 IT 매장(1.5km 거리)이
항상 앞섰다. radius를 늘려도(30km까지 확인) 순서 자체는 안 바뀌고
결과 개수만 늘었다 — 즉 반경 제한 문제가 아니라 정렬 우선순위
문제였다.

편향 없이(중심 좌표 없이) 검색하면 TMAP 자체 정렬로 진짜 에버랜드가
1위로 나오는 것도 확인했다.

### 수정

지도 중심 편향 검색은 그대로 유지하되(일반적인 근처 검색에는
유용함), 편향 없는 검색을 하나 더 호출해서 **검색어와 이름이
정확히 일치하는 결과**를 찾아 맨 앞으로 끌어올린다.

```text
1. 기존처럼 지도 중심 편향 검색 실행
2. 편향 없는 검색을 별도로 1회 더 실행(최대 5건)
3. 그 중 이름이 검색어와 정확히 같은 항목을 중복 없이 맨 앞에 배치
4. 나머지는 기존 편향 검색 결과 순서 그대로 뒤에 붙임
```

### 실제 검증

```text
지도 중심을 서울 용산으로 둔 채 "에버랜드" 검색
수정 전: 1위 서울 용산 IT 매장, 진짜 에버랜드는 순위 밖
수정 후: 1위 에버랜드(경기 용인시 처인구, 테마파크) ✅
        2위부터는 기존과 동일(서울 동명 매장들)
```

검증: 단위 테스트 98개, E2E 테스트 8개 통과.

## 2026-08-08 - "같은 업종만 검색" 필터 추가

전체 LUT 대신, 논의 후 더 직접적인 방법을 택했다. 관광공사 업종
대분류(content_type_id: 12/14/15/25/28/38/39)는 cat2/lcls2보다
항상 더 잘 채워져 있고, 검색 자체를 그 업종으로 제한하는 하드
필터(`content_type_id`)가 이미 백엔드에 다 구현되어 있었다 —
화면에 노출만 안 되어 있었을 뿐이다.

### 발견한 것 하나 더: 음식점이 기본적으로 항상 제외되고 있었음

"올디스타코 을지로본점"(음식점) 근처를 직접 조회해보니 실제로는
음식점이 아주 가깝게 많았다(안동장 166m, 평래옥 216m, 동원집
224m). 그런데 추천 결과는 인현시장·청계 조명기구상가·서울청소년
수련관이었다. 원인은 `is_recommendation_content_type()`가
`include_restaurants=False`일 때 업종 39(음식점)를 통째로
걸러내고 있었고, 화면에서 이 값을 요청에 아예 안 보내서 **어떤
장소를 대체하든 음식점이 후보에 들어올 수 없는 상태**였다.
(관련 테스트 `test_shopping_included_by_default_restaurant_only_with_flag`가
이 정책을 의도적으로 고정하고 있어서, 정책 자체는 그대로 두고
화면에서 옵트인하는 방식으로 풀었다.)

### 추가한 것

1. `SelectedPlace.kto_content_type_id` 필드 추가 — 관광공사 매칭
   시(`/kto-category-match`) 이미 내려주던 `content_type_id`를
   저장해둔다.
2. "관광공사에 아예 매칭이 안 되는 장소" 대응 — 올디스타코처럼
   작거나 신생인 매장은 관광공사에 등록 자체가 안 된 경우가 실제로
   있다(매칭 결과 `matched:false`). 이럴 때는 TMAP 자체 카테고리
   문구(예: "세계요리")에서 업종만 대략 추정하는 작은 키워드
   사전을 프런트에 추가했다 — 전체 세부 카테고리(cat2/lcls2)용
   LUT보다 훨씬 작다(음식점/쇼핑/레포츠/문화시설/관광지 5개
   버킷, 각 대여섯 개 키워드).
3. "대체 추천" 패널에 **"같은 업종만 검색"** 체크박스 추가. 켜면
   `content_type_id`를 하드 필터로 보내고, 대상이 음식점(39)이면
   `include_restaurants: true`도 같이 보낸다(안 보내면 검색은
   음식점만 하고 결과는 전부 걸러지는 모순이 생김).

### 실제 검증

```text
올디스타코 을지로본점(음식점, 관광공사 매칭 안 됨) 추가
→ TMAP 카테고리 "세계요리" → 음식점(39)으로 추정, 체크박스 활성화
→ "같은 업종만 검색" 체크 → 추천 받기
→ 결과: 동원집(83.28점) · 전주식당(78.42점) · 장수촌 풍천장어(73.86점)
  전부 실제 음식점, 전부 도보 거리 이내
```

검증: 단위 테스트 98개, E2E 테스트 8개 통과 (Playwright 실제 화면
조작으로 확인).

## 2026-08-08 - "비슷한 카테고리"·"같은 업종" 체크박스 통합

두 개로 나뉘어 있던 체크박스를 하나("비슷한 곳 우선 추천")로
합쳤다. 매번 사용자가 둘 중 뭘 켤지 고민할 필요 없이, 세부
카테고리 존재 여부로 자동 판단한다.

```text
cat2 또는 lcls2가 있으면 → 카테고리 모드 (소프트 재정렬)
둘 다 없고 kto_content_type_id만 있으면 → 업종 모드 (하드 필터)
둘 다 없으면 → 비활성화
```

체크박스에 `data-mode` 속성("category" | "type" | "")을 저장해서,
"추천 받기" 클릭 시 그 값에 따라 `preferred_cat2`/`preferred_lcls2`
또는 `content_type_id`(+음식점이면 `include_restaurants`)를 보낸다.
백엔드는 그대로 — 프런트에서 어떤 파라미터를 보낼지만 자동 결정.

### 실제 검증

```text
국립중앙박물관(cat2 없음, lcls2=VE07 있음) → 카테고리 모드로 자동 선택
올디스타코 을지로본점(관광공사 매칭 자체 안 됨, 업종만 추정됨) → 업종 모드로 자동 선택
  → 체크 후 추천: 동원집·전주식당·장수촌 풍천장어 (전부 음식점)
```

검증: 단위 테스트 98개, E2E 테스트 8개 통과.

## 2026-08-08 - 세부 카테고리 없는 장소는 업종 필터 강제 적용

세부 카테고리(cat2/lcls2)가 아예 없는 장소(올디스타코처럼 관광공사
매칭 자체가 안 되는 곳)는 업종 필터 없이 추천하면 엉뚱한 결과(시장·
상가·청소년수련관)가 나오는 걸 이미 확인했었다. 이제 이 경우엔
"비슷한 곳 우선 추천" 체크박스를 **선택이 아니라 강제**로 바꿨다.

```text
category 모드(cat2/lcls2 있음): 기존과 동일 — 선택, 사용자가 켜고 끌 수 있음
type 모드(업종만 있음):        체크됨 상태로 고정, 끌 수 없음
정보 아예 없음:                 비활성화, 항상 꺼짐 (기존과 동일)
```

체크박스의 `disabled`는 이제 "정보가 없어서 못 씀"이 아니라
"강제 적용이라 못 끔"의 의미도 겸하게 돼서, 실행 버튼 쪽 로직은
`disabled` 여부를 더 이상 보지 않고 `checked` 값만 본다.

### 실제 검증

```text
올디스타코 을지로본점 추가 → 체크박스 자동 checked=true, disabled=true
사용자가 아무것도 안 누르고 바로 "추천 받기"만 클릭
→ 결과: 동원집·전주식당·장수촌 풍천장어 (전부 음식점, 자동 적용됨)

국립중앙박물관(lcls2 있음) → 여전히 checked=false, disabled=false
(카테고리 모드는 그대로 선택 사항 유지)
```

검증: 단위 테스트 98개, E2E 테스트 8개 통과.

## 2026-08-08 - "이전 일정 기준 검색" 범위를 지도에 원으로 표시

"이전 일정 기준으로 검색"이 글로만 설명돼 있어서 실제로 어디를
얼마나 넓게 찾는 건지 감이 안 온다는 피드백을 받았다. 검색 중심과
반경을 지도 위에 파란 점선 원 + 🔍 마커로 실제로 그려서 눈으로
바로 확인할 수 있게 했다.

```text
체크 꺼짐: 대체할 그 장소 자신 기준, 기본 반경 3km(백엔드 기본값과 동일)
체크 켜짐: 이전 일정 기준, 반경은 이전 일정↔대체할 장소 사이 거리
           (300m~20km로 제한, 화면에 이미 뜨던 계산식 그대로)
```

체크박스를 토글할 때마다 원이 다시 그려지고, 지도가 그 원에 맞춰
자동으로 이동·줌된다(`map.fitBounds`). 패널을 닫으면 원도 같이
지운다.

### 실제 검증

```text
경복궁 → 국립중앙박물관 순서로 일정 추가, 국립중앙박물관 대체 추천 열기
꺼짐: 원 중심 = 국립중앙박물관 좌표, 반경 3000m
켜짐: 원 중심 = 경복궁 좌표, 반경 5943m (실제 두 장소 간 거리)
Leaflet 레이어 직접 조회해서 중심·반경이 안내 문구와 정확히 일치하는 것 확인
```

검증: 단위 테스트 98개, E2E 테스트 8개 통과.

## 2026-08-08 - 추천 후보 설명을 TMAP 소개문으로 (관광공사 대체)

관광공사 overview는 백과사전식으로 길고 딱딱한 경우가 많다. TMAP
장소 검색 응답에 원래 있던 `desc` 필드를 확인해보니 방송 소개
위주의 짧고 캐주얼한 소개문이 들어있었다(예: "tvN 수요미식회에서
소개한 안동장은..."). 이제 후보 카드에는 이걸 우선 쓴다.

```text
GET /place-search 응답에 없던 desc 필드를 이제 파싱한다
후보 이름+좌표로 TMAP을 다시 검색(반경 5km)해서 이름이 일치하고
가장 가까운 결과의 desc를 가져온다
desc가 없는 장소(예: DDP)는 기존처럼 관광공사 overview로 자동 대체
최종 추천 후보(보통 3개)만 조회한다 — 제외된 후보까지 하면 낭비
```

`RankedTourCandidate`가 아니라 `/recommendations` 처리 중에
후보별로 별도 TMAP 검색을 추가로 호출하는 방식이라, 캐싱은 아직
안 걸었다(호출 수가 최대 3회라 우선 단순하게 구현, 느리면 추후
캐시 추가 고려).

### 실제 검증

```text
올디스타코 을지로본점 대체 추천 3개 전부 실제 API로 확인
1. 동원집 → TMAP 소개문 표시 ("KBS 2TV생생정보... 맛집…")
2. 전주식당 → TMAP 소개문 표시 ("SBS 생방송투데이... 연탄생선구이 전문점...")
3. 장수촌 풍천장어 → TMAP에 소개문 없어서 관광공사 overview로 자동 대체됨
```

검증: 단위 테스트 98개, E2E 테스트 8개 통과.

## 2026-08-08 - 추천 후보 카드 시인성 개선 + 설명 전체보기 팝업

후보 카드가 이름·점수·주소·이동정보·소개문이 전부 같은 톤의 회색
텍스트로 나열되어 있어 구분이 잘 안 됐다. 칸을 나누고, 관광공사
원문 전체는 팝업으로 볼 수 있게 했다.

```text
rec-card-header : 순위 배지 + 이름(굵게) + 점수 배지 + 카테고리 배지
rec-card-meta   : 주소 · 이동시간 · 혼잡도 (연회색 박스로 묶음)
rec-card-desc   : 짧은 소개문(TMAP 우선) — 옅은 베이지 박스로 구분
                  "관광공사 설명 전체보기" 버튼 포함(overview 있을 때만)
```

"전체보기" 클릭 시 화면 중앙에 팝업(모달)이 뜨고, 장소 이름을
제목으로, 관광공사 원문 overview 전체(자르지 않음)를 본문으로
보여준다. 배경 클릭·닫기 버튼·Esc 키 전부 닫기로 동작한다.

### 실제 검증

```text
올디스타코 대체 추천 카드 3개 모두 새 레이아웃으로 렌더링 확인
"관광공사 설명 전체보기" 버튼 3개 전부 존재 확인
"동원집" 버튼 클릭 → 팝업 제목 "동원집", 본문 263자 전체 텍스트 표시
Esc 키로 팝업 정상 닫힘 확인
```

검증: 단위 테스트 98개, E2E 테스트 8개 통과.

## 2026-08-08 - 설명 전체보기 버튼 문구·우선순위 조정

버튼 문구를 "관광공사 설명 전체보기" → **"설명 전체보기"**로
줄였다. 그리고 팝업 내용도 카드에 이미 보이는 짧은 소개문과 같은
우선순위를 쓰도록 바꿨다 — TMAP 소개문이 있으면 그걸 전체보기로,
없는 장소만 관광공사 overview를 보여준다.

```text
기존: 카드엔 TMAP 우선 표시, 팝업은 항상 관광공사 overview만 표시
변경: 카드·팝업 둘 다 TMAP 소개문 우선, 없으면 관광공사 overview
```

"전체보기" 버튼도 실제로 잘린 내용이 있을 때만 뜨도록 조건을
바꿨다(짧은 소개문 길이 == 원문 길이면 더 볼 게 없으므로 버튼 숨김).

### 실제 검증

```text
버튼 문구 3개 전부 "설명 전체보기" 확인
동원집 팝업 본문이 TMAP 소개문(174자, "KBS 2TV생생정보...")으로
바뀐 것 확인 (이전엔 관광공사 overview 263자였음)
```

검증: 단위 테스트 98개, E2E 테스트 8개 통과.

## 2026-08-27 - 백엔드 Render 배포 + api-base 도메인 분리 대응

백엔드(`plan_b_api`)를 Render Web Service로 배포했다.
`https://alternative-travel-destination.onrender.com` 에서 서비스 중.

- `render.yaml`: Blueprint용 설정 (하지만 실제로는 대시보드에서
  Web Service를 수동 생성 — Blueprint 메뉴가 안 보이는 Render 계정이라
  Build/Start Command·Health Check·환경변수를 직접 입력함).
  Start Command: `python -X utf8 -m plan_b_api.server --host 0.0.0.0 --port $PORT`
- 외부 의존성이 없어 Build Command는 `echo "no external dependencies"`로 no-op 처리.
- API 키들은 `sync: false`로 선언해 값이 repo에 안 들어가고 Render
  대시보드 환경변수로만 입력되게 함.

`web/index.html`의 `#api-base` 기본값을 Render 주소로 바꿨는데,
페이지 초기화 스크립트가 무조건 `location.origin`으로 덮어쓰고
있어서 그대로 두면 프론트를 별도 도메인(Vercel)에 올렸을 때 자기
자신을 백엔드로 착각해 API 호출이 다 깨지는 문제를 발견했다.
그래서 `location.hostname`이 `localhost`/`127.0.0.1`이거나
`.onrender.com`으로 끝날 때만(=백엔드가 직접 페이지를 서빙 중일
때만) `location.origin`으로 덮어쓰고, 그 외(Vercel 등 프론트
전용 정적 호스팅)에는 하드코딩된 Render 주소를 그대로 쓰도록 수정.

### 실제 검증

```text
Render 배포 로그: "Your service is live", /health 200 OK,
integrations 전부 true (TMAP/관광공사/기상청/ODsay 등 키 정상 인식)
```

검증: 단위 테스트 98개, E2E 테스트 8개 통과 (localhost 자동-origin
경로가 그대로 유지되는지 재확인).

## 2026-08-27 - Vercel 프론트 배포 완료

`web/index.html`을 Vercel에 정적 배포. Root Directory `web`,
Framework Preset "Other"로 설정.

- Vercel 프로젝트가 `main` 브랜치를 보고 있어서(우리 수정은 전부
  `feature/recommend-category-filter`에만 있음) 처음엔 옛날 코드가
  배포됨 → Settings → Git → Deploy Hooks로
  `feature/recommend-category-filter` 전용 훅 생성 후 그걸로 트리거.
- 첫 트리거 시 "Deployment Blocked: commit author did not have
  contributing access... Hobby Plan does not support collaboration
  for private repositories" 에러 → GitHub repo를 Public으로 전환해서
  해결 (`.env`/`.cache`는 이미 gitignore라 키 노출 없음).
- 배포 URL은 Vercel Deployment Protection(SSO)이 걸려있어 팀 로그인
  없이는 접속 불가 — 필요하면 Settings → Deployment Protection에서
  해제 가능.

### 실제 검증

```text
Vercel에서 검색 실행 → Render 백엔드(alternative-travel-destination
.onrender.com)로 요청 정상 전달, 검색 결과 정상 표시 확인
```

## 2026-08-27 - 지도 타일 "API KEY REQUIRED" 워터마크 수정

지도에 "API KEY REQUIRED" 글자가 박힌 타일이 뜨는 문제 발견.
CARTO(`basemaps.cartocdn.com`)가 정책을 바꿔서 이제 API 키 없이
호출하면 타일 이미지 자체에 그 문구를 찍어서 반환함 — 코드 버그가
아니라 서비스 쪽 정책 변경이고, 배포본에 접속하는 모두에게 똑같이
발생하는 문제였음 (`curl`로 CARTO 타일 URL 직접 호출해서 실제
워터마크 이미지 확인함).

키가 필요 없는 **OpenStreetMap 표준 타일 서버**로 교체.

```text
기존: https://{s}.basemaps.cartocdn.com/rastertiles/voyager/{z}/{x}/{y}{r}.png
변경: https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png
```

### 실제 검증

```text
curl로 OSM 타일 직접 다운로드 → 정상 지도 이미지 확인 (워터마크 없음)
E2E 테스트 8개 통과 (지도 렌더링 포함된 모바일 뷰포트 테스트 포함)
```

참고: OSM 타일 서버는 개인/저트래픽 용도로는 무료지만, 트래픽이
많아지면 자체 정책상 차단될 수 있음(공식 사용 정책 있음) — 나중에
사용자가 많아지면 상용 타일 서비스(키 발급) 검토 필요.

## 2026-08-27 - 장소 검색 카테고리 뱃지 "박물관\/기념관" 표시 오류 수정

"국립중앙박물관" 검색 시 카테고리 뱃지가 "박물관\/기념관"처럼
슬래시 앞에 백슬래시가 그대로 보이는 문제 발견.

원인 확인: TMAP API 응답의 `lowerBizName` 같은 필드가 JSON 이스케이프
문제가 아니라 **원본 데이터 자체에 실제 백슬래시 문자**를 포함하고
있음 (`curl`/직접 클라이언트 호출로 UTF-8 바이트까지 까봐서 확인 —
`\xea\xb4\x80\\/\xea\xb8\xb0` 처럼 0x5C(백슬래시) 바이트가 실제로
들어있었음). 우리 JSON 파싱(`json.loads`)은 정상 동작 중이었고,
TMAP 쪽 데이터 특이사항이었음.

`place_search_client.py`의 `_text()` 헬퍼에서 모든 TMAP 텍스트
필드 추출 시 `"\\/"` → `"/"`로 정리하도록 수정 (category/주소/설명
등 필드 전부에 공통 적용).

### 실제 검증

```text
TMAP 클라이언트로 "국립중앙박물관" 직접 조회 → category 값이
"박물관\/기념관"(백슬래시 포함) → 수정 후 "박물관/기념관"(정상)로
바뀐 것 UTF-8 바이트 단위까지 확인
```

회귀 테스트 추가(`test_category_strips_stray_backslash_before_slash`).
검증: 단위 테스트 99개, E2E 테스트 8개 통과.
